# CLAUDE.md

Это новый репозиторий веб-приложения для чтения и скачивания ранобэ поверх
`ranobelib-python-sdk`. Полное ТЗ — в `TASK.md` в корне репозитория (перенеси туда
исходный документ, если его там ещё нет). Этот файл — оперативные инструкции для работы
в кодовой базе; при противоречии с ТЗ приоритет у ТЗ, а несоответствия — повод остановиться
и спросить, а не додумать самому.

## Что это за проект

Тонкий веб-слой поверх стабильного SDK (`ranobelib` на PyPI, `ranobelib-python-sdk>=0.3.0`).
Вся логика похода в API ranobelib.me, ретраи, рейт-лимитинг, кэш сырых ответов, парсинг
контента и генерация epub/fb2/txt/pdf — уже в SDK. Задача этого репозитория — UI и HTTP-слой
вокруг него, ничего из вышеперечисленного не дублировать и не переизобретать.

Golden rule: если чувствуешь, что пишешь код, который обходит SDK, добавляет параллельный
кэш тех же данных или сам сортирует/парсит номера глав — остановись, это, скорее всего,
уже сделано в SDK и его нужно просто вызвать.

### SDK — живая зависимость, не заморозка на 0.3.0

`ranobelib-python-sdk` активно разрабатывается, в нём могут появляться новые методы/поля/
возможности после `0.3.0`. Из этого следуют два обязательных правила, оба — до, а не после
того, как начал писать код:

1. **Перед тем как реализовывать что-либо своими руками на уровне сайта** (парсинг, доп.
   поле метаданных, недостающий фильтр, любую логику, которая выглядит как «логика про
   ранобэ», а не «логика веб-слоя») — сначала проверить PyPI, нет ли уже нужного в более
   свежей версии SDK, чем та, что закреплена в `pyproject.toml` (`pip index versions
   ranobelib-python-sdk`, страница пакета на pypi.org, `CHANGELOG.md` SDK). Не изобретать
   решение самостоятельно, не проверив это в первую очередь.
2. **Если нужной возможности нет даже в последней версии SDK** — завести issue в репозитории
   SDK: https://github.com/LLIJIYAPNIK/ranobelib-python-sdk/issues — что нужно и зачем,
   желательно с предлагаемой сигнатурой метода/примером использования, как остальной
   публичный API SDK. Не молча реализовывать эту логику внутри сайта в обход SDK. Сначала
   issue, потом — при необходимости — временный воркэраунд на сайте, явно помеченный как
   временный до появления фичи в SDK (например TODO-комментарием со ссылкой на issue).

Это тот же Golden rule, только сдвинутый по времени: не только «не дублируй то, что уже
есть в SDK», но и «не дублируй то, что скоро может появиться в SDK, вместо того чтобы это
туда предложить». Вся предметная логика про ранобэ (парсинг контента, номера глав,
экспорт, работа с API ranobelib.me) должна жить в SDK — этот репозиторий как был, так и
остаётся тонкой обёрткой вокруг него, а не местом, куда постепенно перетекает бизнес-логика.

## Стек и инструменты

- **Backend**: FastAPI (async, естественно сочетается с httpx/pydantic v2 стеком SDK).
  Не оборачивать вызовы SDK в `asyncio.run()` или thread-пулы — весь путь запроса должен
  быть async.
- **Пакетный менеджер**: `uv`. Зависимости — `uv add`, запуск — `uv run`.
- **Зависимость на SDK**: обычная PyPI-зависимость, `ranobelib-python-sdk>=0.3.0`. Никогда
  не переключать на path/git-зависимость на локальную копию SDK-репозитория, даже временно
  для отладки — если кажется, что в SDK баг, завести это отдельно, а не патчить его исходники
  внутри этого репозитория.
- **Фоновые задачи**: для MVP — in-memory job-очередь (однопроцессный деплой). Если понадобится
  горизонтальное масштабирование — Redis/RQ/Celery, но не вводить эту зависимость заранее
  без явной необходимости.
- **Frontend**: сервер-рендеринг (Jinja2) + минимум JS для интерактивности (выбор глав
  чекбоксами, поллинг/SSE прогресса долгих операций). Тяжёлый SPA-фреймворк не нужен, если
  не появится веская причина. Визуальная реализация экранов — не изобретать вёрстку заново,
  импортировать из Claude Design (см. раздел «Дизайн» ниже); `DESIGN_PROMPT.md` в корне
  репозитория — исходный промпт, из которого получены три концепции, оставлен для истории/
  контекста, актуальный источник вёрстки — сам файл концепции в Claude Design.
- **Данные приложения (пользователи, личная библиотека, активность, история загрузок)**:
  отдельная БД приложения (SQLite для MVP, `app/db/`) — не путать с SDK `cache_dir`, который
  остаётся публичным общим кэшем сырых ответов ranobelib.me. Хэш паролей — passlib/bcrypt,
  серверные сессии — стандартный session cookie (например `itsdangerous`/Starlette
  `SessionMiddleware`), без сторонних OAuth-провайдеров и без какой-либо интеграции с
  аккаунтом на ranobelib.me.

## Дизайн

UI спроектирован в Claude Design (три концепции, см. `DESIGN_PROMPT.md`); для реализации
выбрана концепция 3 — «Библиотечная утилита» (`Concept 3 - Media Collection Dashboard`).
Импортировать её нужно через `claude_design` MCP, а не восстанавливать вёрстку по
скриншотам/памяти:

```
Use the claude_design MCP (https://api.anthropic.com/v1/design/mcp, auth via /design-login)
to import this project:
https://claude.ai/design/p/bf4a9698-ecd7-42c6-be33-f2b165ab90eb?file=Concept+3+-+Media+Collection+Dashboard.dc.html

Focus on these files (the whole project is readable):
- `Concept 3 - Media Collection Dashboard.dc.html`

Also read these files the selection imports:
- `support.js`

Implement: `Concept 3 - Media Collection Dashboard.dc.html`
```

Правила при реализации:

- Если `claude_design` MCP не залогинен — сначала выполнить `/design-login`, не пытаться
  обойтись без него (не реконструировать дизайн вручную).
- Импортировать/читать весь проект по ссылке доступен целиком (все три концепции), но
  реализовывать в этом репозитории — только `Concept 3 - Media Collection Dashboard.dc.html`
  и то, что она импортирует (`support.js`). Концепции 1 и 2 не портировать в код, если явно
  не попросят.
- Вёрстка/стили/компоненты берутся из импортированного файла как есть (структура экранов,
  брейкпоинты десктоп/мобайл, цвета, типографика) — переносятся на Jinja2-шаблоны и статику
  проекта, а не переизобретаются заново «в духе» дизайна.
- Импортированный HTML/JS — это витрина экранов с плейсхолдер-данными; при переносе в
  Jinja2-шаблоны плейсхолдеры заменяются на реальные Jinja2-переменные/циклы по данным,
  которые отдаёт `services/` (см. «Архитектура» ниже), поведение (чекбоксы выбора глав,
  прогресс-бар, модалки выбора перевода и т.д.) — на реальные вызовы API этого приложения,
  а не оставляется статичным.

## Архитектура

```
app/
  api/            # HTTP-роуты (тонкие, без бизнес-логики)
  services/        # обёртка над RanobeLib: единственное место, где вызывается SDK
  auth/             # регистрация/логин/логаут, хэш паролей, сессии — уровень приложения,
                     # никак не связано с аккаунтом на ranobelib.me
  db/               # модели и доступ к БД приложения (пользователи, личная библиотека,
                     # активность, история загрузок) — отдельно от SDK cache_dir
  exceptions.py     # маппинг RanobeLibError → HTTP-статус/сообщение
  jobs/             # фоновые задачи для download_title() и массового экспорта
  templates/        # Jinja2-шаблоны экранов
  static/
tests/
```

Ключевой принцип: SDK вызывается только из `services/`. Роуты не создают `RanobeLib(...)`
напрямую и не ловят `RanobeLibError` — это делает единый middleware/exception handler
(раздел «Обработка ошибок» ниже), а не каждый обработчик по отдельности.

`db/` (БД приложения) и SDK `cache_dir` — разные вещи и не должны смешиваться: `cache_dir`
хранит публичный кэш сырых ответов ranobelib.me, общий для всех пользователей; `db/`
хранит приватные данные приложения (кто есть кто, у кого что в личной библиотеке, кто что
сегодня читал/скачивал). Не писать пользовательские данные в `cache_dir` и не пытаться
обойтись без БД, храня их поверх SDK-кэша.

## Обязательные решения из ТЗ (не переоткрывать по ходу разработки)

Эти решения уже приняты в ТЗ — не спрашивать их заново, реализовывать как есть:

- **Кэш** — один общий `cache_dir` на всё приложение (не per-user/per-session), т.к. данные
  публичные и общие для всех пользователей. `cache_dir` — на персистентном volume при
  деплое в контейнере, иначе кэш обнуляется на каждом рестарте. `cache_ttl` подобрать так,
  чтобы новая глава на проде была видна без ручного `refresh=True` в разумный срок (часы,
  не дни) — значение явно закладывается в конфиг, не хардкодится.
- **Долгие операции** (`download_title()`, экспорт многих глав) — background job + прогресс
  (polling или SSE), не синхронный HTTP-запрос, который висит до готовности. Индикатор
  «глава N из M» обязателен для `download_title()`. Для одной главы/тома — синхронная отдача
  приемлема, если не появится причина решить иначе.
- **Экспорт** — временный файл экспорта удаляется после отдачи ответа (либо отдаётся из
  памяти/потока). Не оставлять мусор в `/tmp` между запросами.
- **Форматы экспорта** — брать из `ranobelib.exporters.EXPORTERS` динамически, никогда не
  хардкодить список `["txt", "fb2", "epub", "pdf"]`. Если `"pdf"` отсутствует в словаре
  (нет WeasyPrint на сервере) — просто не показывать эту опцию в UI, не падать и не
  выводить ошибку.
- **Неоднозначные переводы** (`MultipleTranslationsError` / `MultipleTitleTranslationsError`)
  — сайт обязан явно предложить выбор пользователю, никогда не выбирать `branch_id`/
  `translation_index` сам по умолчанию (тот же принцип, что и в самом SDK).
- **Авторизация** — своя, уровня приложения: email/пароль, регистрация/логин/логаут,
  серверные сессии (см. `app/auth/`). Нужна для персональной библиотеки и активности
  (PR 13+). Никак не связана с аккаунтом на ranobelib.me и не пытается его эмулировать —
  это раскрывает старое решение «не реализовывать авторизацию», которое имело в виду именно
  логин на ranobelib.me через SDK. `AuthRequiredError` от SDK (недоступный на сайте контент)
  по-прежнему просто «недоступно» — это другая сущность, не наша авторизация.
- **Оставшееся время загрузки** («Загрузки», PR 17) — считается на web-слое, по таймингу
  между тиками `on_chapter(completed, total)` из `download_title()`. Отдельного issue в SDK
  для этого заводить не нужно: SDK уже отдаёт прогресс по главам, экстраполяция оставшегося
  времени — обычная логика веб-слоя, не про ранобэ.
- **Параллельные запросы к API в обход SDK** — запрещены. Не создавать несколько
  `RanobeLib`-инстансов на один тайтл ради ускорения, не добавлять свой семафор/пул поверх
  `ApiClient` — он уже рейт-лимитирует и пейсит запросы.

## Обработка ошибок

Один центральный маппинг `RanobeLibError` → HTTP-ответ (exception handler в FastAPI, не
try/except в каждом роуте):

| Исключение SDK | HTTP | Сообщение пользователю |
|---|---|---|
| `TitleNotFoundError` | 404 | «Тайтл не найден, проверьте ссылку» |
| `ChapterNotFoundError` | 404 | «Глава не найдена» |
| `VolumeNotFoundError` | 404 | «Том не найден» |
| `MultipleTranslationsError` | 300/409 + список вариантов | UI выбора перевода |
| `MultipleTitleTranslationsError` | 300/409 + список вариантов | UI выбора по каждой неоднозначной главе / общий branch_id на тайтл |
| `AuthRequiredError` | 403 | «Требуется авторизация — недоступно» |
| `RateLimitError` | 429 | «ranobelib сейчас ограничивает запросы, попробуйте позже» |

Технические детали исключения — в логи сервера, никогда в тело ответа пользователю.

## Roadmap

Продукт внедряется фичами по порядку ниже. **Каждая фича из этого списка — отдельный PR.
Внутри PR — атомарные коммиты**, по одному на логический шаг (перечислены под каждой
фичей) — не сваливать фичу в один коммит и не смешивать несколько фич в одном PR. Порядок
важен: PR 2–3 — инфраструктурные и нужны почти всем остальным; PR 7 (выбор перевода)
технически нужен и чтению (PR 6), и экспорту (PR 8), и скачиванию тайтла (PR 10) — их можно
начать раньше, но смержить только после PR 7 или явно замокать выбор перевода временно.

Пункты 13–19 — вторая волна (личный кабинет, библиотека, загрузки, активность, мобильная
адаптация), добавлены поверх MVP (1–12). PR 13 (авторизация приложения) — инфраструктурный
для этой волны, от него зависят PR 14, 17, 18. PR 15 («Все тайтлы») заблокирован до появления
метода листинга каталога в самом SDK (см. сам пункт) и не блокирует остальные пункты волны —
его можно оставить в самом конце и просто ждать. PR 19 (мобильная адаптация) логично делать
последним в волне, когда все новые экраны уже существуют, чтобы не адаптировать вслепую.

Пункты 20–41 — третья волна: багфиксы и доработки UX, найденные уже после вывода MVP и
второй волны в реальную эксплуатацию. Друг от друга не зависят и порядок внутри волны не
принципиален — можно брать в любой последовательности, кроме отдельно отмеченных
зависимостей: PR 31 наследует блокер PR 15 на листинг каталога в SDK; PR 34 делать после
PR 33 — обе дорабатывают одну и ту же страницу настроек чтения, PR 33 её создаёт, PR 34
наполняет; PR 38 заблокирован до появления в SDK метода листинга жанров (см. сам пункт),
аналогично PR 15/31.

Пункты 42–47 — четвёртая волна: анимации и полировка ощущений (motion/UX-polish) поверх
уже реализованных экранов — отдельная тема от багфиксов третьей волны. Друг от друга не
зависят, порядок внутри волны не принципиален, кроме отдельно отмеченных зависимостей:
PR 46 технически удобнее делать после PR 39 (анимирует переход между разделами уже
разворачиваемого бургер-сайдбара); PR 43 имеет смысл только после того, как расчёт веса
новеллы из PR 29 уже реализован, иначе анимировать нечего.

Пункты 48–60 — пятая волна: баги и доработки, найденные уже после четвёртой волны
(анимаций) в реальной эксплуатации. Друг от друга не зависят, порядок внутри волны не
принципиален, кроме отдельно отмеченных зависимостей: PR 51 диагностирует пропадание
плавающей панели экспорта с оглядкой на фиксированные мобильные элементы (шапка аккаунта/
таб-бар), появившиеся в PR 46, — имеет смысл делать после него, если ещё не сделан; PR 57
и PR 58 оба трогают `download_history` (таблицу и `_download_history.html`) — независимы
друг от друга, но при параллельной разработке стоит согласовать миграции, чтобы не
конфликтовать в одной таблице.

Пункты 61–66 — шестая волна: не багфиксы, а новая продуктовая фича (чтение тапом) плюс
подготовка настроек чтения под её появление. Порядок внутри волны важен: PR 62 (сама
механика) не зависит от остальных и может идти первым; PR 63 (переключатель в настройках)
нужен и PR 61 (разделы в настройках, куда кладётся переключатель), и PR 62 (без механики
переключать нечего); PR 64 и PR 65 (кастомизация — стиль выделения абзаца и анимация
появления) оба требуют готового PR 62 и обычно делаются после PR 63, но независимы друг от
друга; PR 66 (полноэкранный просмотр изображений) не зависит от механики чтения тапом
технически, но должен знать о состоянии переключателя из PR 63, чтобы отключать себя в
этом режиме — делать после PR 63.

Пункт 67 — отдельный баг, обнаруженный в тот же период, не связанный с чтением тапом и без
зависимостей от остальных пунктов волны: общее sqlite3-соединение приложения не
потокобезопасно и валит страницу «Загрузки» под её собственным поллингом. Не блокирует и не
блокируется остальными пунктами шестой волны — можно (и стоит, учитывая серьёзность) брать
вне очереди.

Пункты 68–76 — седьмая волна: баги и доработки, найденные уже после того, как чтение тапом
(шестая волна) выехало в реальную эксплуатацию. Друг от друга не зависят, кроме пунктов,
трогающих один и тот же файл `tap-to-read.js`: PR 73 (тап-зона на весь экран) стоит делать
раньше PR 74 (полноэкранный просмотр изображений в этом режиме) — расширенный обработчик
тапа из PR 73 должен научиться исключать клик по изображению, прежде чем на изображение
вообще имеет смысл вешать открытие лайтбокса; PR 75 и PR 76 (переход по тапу после
последней главы и автоскролл) правок в PR 73/74 не требуют, но при параллельной разработке
стоит согласовать конфликты в одном файле.

Пункты 77–79 — восьмая волна: скорость чтения для режима тапа — новая продуктовая фича, а
не багфикс, поверх уже готового PR 62/65. PR 77 (тест скорости) и PR 78 (ручной ввод
скорости) производят одно и то же значение `readingSpeedWpm` в настройках и независимы друг
от друга — можно делать в любом порядке, PR 78 даже раньше PR 77, тест не единственный
способ задать скорость (см. сам пункт 78). PR 79 (сами темп-анимации по скорости) нуждается
хотя бы в одном источнике этого значения, поэтому делается после хотя бы одного из PR 77/78.

Пункты 80–87 — девятая волна: полировка UX на страницах аутентификации, прогресс чтения по
главам в оглавлении и расширение фильтрации каталога (полноценная панель «Фильтры», теги,
авторы/художники/переводчики). Друг от друга не зависят, кроме: PR 84 переиспользует
компонент отображения прогресса из PR 83, делать после него. PR 85–87 расширяют каталог/
страницу тайтла в разных, не пересекающихся направлениях и у каждого свой отдельный SDK-
блокер (см. сами пункты) — независимы друг от друга и от остальных пунктов волны.

Пункты 88–93 — десятая волна: не багфиксы, а новая продуктовая фича — профиль пользователя
(аватар вместо email в шапке, страница профиля, вкладки настроек, поля аккаунта, смена
пароля). Чистый веб-слой/БД приложения (`app/db`, `app/auth`) — SDK и Golden rule эту волну
не затрагивают, в приложении и так уже есть отдельная БД для пользовательских данных (см.
«Архитектура»). Порядок внутри волны важен: PR 88 (аватар вместо email/«Выйти») не зависит
от остальных и может идти первым; PR 89 (вкладки настроек) тоже независим и может идти
параллельно; PR 90 (поля аккаунта) и PR 91 (смена пароля) оба кладут содержимое во вкладки,
созданные PR 89, — делаются после него; PR 92 (страница профиля) показывает никнейм/bio из
PR 90, поэтому осмысленна только после него; PR 93 (выпадающее меню профиля) ссылается на
все конечные страницы остальных пунктов волны («Профиль» → PR 92, «Настройки» → PR 89) и
поэтому логично идёт последним, хотя технически привязан только к аватару из PR 88.

Пункты 94–106 — одиннадцатая волна: доработки и баги, найденные после того, как десятая
волна (профиль) и панель «Фильтры» (PR 85) выехали в реальную эксплуатацию, плюс два шага
онбординга при регистрации. Друг от друга не зависят, кроме: PR 98 (кнопка «Фильтры» вместо
всегда закреплённой панели) делать первым среди PR 98–100/103 — PR 99 (сворачиваемые секции)
и PR 103 (мобильная полноэкранная модалка) оба переиспользуют переключаемую панель, которую
вводит PR 98, а не саму `.catalog-filters` в её нынешнем виде; PR 100 (страна — чекбоксы,
семантика «ИЛИ») технически независим от 98/99/103, но касается той же панели — логично
делать в этом же диапазоне. PR 104 (мобильные настройки — сначала список разделов) не
обязан, но логично идёт после PR 95 (алфавитный порядок разделов), чтобы список разделов на
телефоне сразу был в правильном порядке. PR 106 (предложить сменить аватар после регистрации)
требует готового PR 96 (сама загрузка аватара) и естественно идёт после PR 105 (никнейм при
регистрации) по потоку экранов, хотя технически 105 и 106 друг от друга не зависят.

Пункты 107–118 — двенадцатая волна: баги и доработки, найденные после того, как десятая и
одиннадцатая волны выехали в реальную эксплуатацию. Друг от друга не зависят, кроме: PR 111,
112, 113, 114 и 118 все правят одну и ту же область — шапку каталога и `.catalog-filters`
(PR 85/98/99/100) — независимы друг от друга по функциональности, но при параллельной
разработке стоит согласовать, чтобы не конфликтовать в одних и тех же CSS-блоках.

Пункты 119–130 — тринадцатая волна: доработки и новая фича (публичный профиль) поверх
двенадцатой волны. Друг от друга не зависят, кроме: PR 122 (страница профиля, пересматривает
PR 92) идёт первым среди PR 122–124 — PR 123 (избранный тайтл) и PR 124 (настройки
приватности) оба показываются/скрываются на странице профиля из PR 122, а PR 124
дополнительно зависит от PR 123 (переключатель видимости избранного тайтла не имеет смысла
без самой фичи). PR 130 объединяет два отдельных сообщения пользователя об одном и том же
баге (баг с уже отправленного скриншота и переработанное желаемое поведение с новым
скриншотом-референсом) — один PR, а не два. Пункт 127 (кнопка «наверх» в каталоге смещается
при открытой панели фильтров) не зависит от пунктов 113/114 (двенадцатая волна) технически,
но правит тот же угол экрана — логично проверить после них, если они ещё не сделаны.

Пункты 131–136 — четырнадцатая волна: новая продуктовая фича (реакции и комментарии к
абзацам) плюс её последствия для настроек чтения и профиля. Чистый веб-слой — комментарии и
реакции не имеют отношения к контенту ranobelib.me как таковому (это данные приложения о
приложении, а не о ранобэ), поэтому Golden Rule и проверка SDK здесь неприменимы, в отличие
от большинства предыдущих фич. Порядок внутри волны важен: PR 131 (контекстное меню) не
зависит от остальных и идёт первым — PR 132 (реакции) и PR 133 (комментарии) оба вызываются
из этого меню и бессмысленны без него; PR 132 и 133 между собой не зависят и могут делаться в
любом порядке. PR 134 (настройка «скрыть реакции и комментарии») требует готовых PR 132 и 133
— скрывать пока нечего. PR 135 (счётчик сообщений в профиле) требует PR 133 (для комментариев
считать). PR 136 (календарь чтения) не зависит от остальных пунктов волны — источник данных
(`activity_events`, PR 18) уже существует независимо от комментариев/реакций.

Пункты 137–144 — пятнадцатая волна: баги и доработки, найденные после того, как четырнадцатая
волна (реакции/комментарии/профиль) выехала в реальную эксплуатацию. Друг от друга не
зависят, кроме: PR 139 и PR 140 оба правят `.reading-calendar` (PR 136) — независимы по
функциональности (форма грида и содержимое тултипа — разные причины), но при параллельной
разработке стоит согласовать, чтобы не конфликтовать в одном CSS-блоке/шаблоне.

1. **Каркас проекта** — `uv init`, зависимость `ranobelib-python-sdk>=0.3.0`, скелет
   FastAPI-приложения (`app/api`, `app/services`, `app/exceptions.py`, `app/jobs`,
   `app/templates`, `app/static`), health-check роут, базовый CI/lint. Коммиты: init
   проекта → зависимости → скелет FastAPI → health-check → CI.

2. **Слой обращения к SDK и конфиг кэша** — `services/` как единственное место, создающее
   `RanobeLib(...)`; конфиг `cache_dir`/`cache_ttl` из окружения; заметка/докс про
   персистентный volume при деплое. Коммиты: клиент-обёртка → конфиг кэша → smoke-тест на
   мок SDK.

3. **Обработка ошибок SDK** — маппинг из таблицы в `exceptions.py`, единый FastAPI
   exception handler. Коммиты: маппинг исключений → handler → тесты на каждое исключение
   из таблицы.

4. **Поиск/открытие тайтла** — форма ввода ссылки/`{id}--{slug}`, вызов `get_info()`,
   `TitleNotFoundError` идёт через готовый handler из PR 3. Коммиты: роут и форма →
   вызов SDK → обработка «не найден» → (опц.) заготовка под историю тайтлов.

5. **Страница тайтла и оглавление** — рендер метаданных, `get_table_of_contents()`
   (сортировка не трогается, приходит готовой), бейдж «N переводов», чекбоксы выбора глав
   (UI, без самого экспорта). Коммиты: метаданные тайтла → оглавление тома/главы → бейдж
   переводов → чекбоксы выбора (UI-заготовка под PR 8).

6. **Онлайн-чтение главы** — `get_chapter()`, рендер контента с явным решением по
   экранированию, навигация пред./след. глава, `ChapterNotFoundError`/`VolumeNotFoundError`.
   Коммиты: роут чтения → рендер контента и решение по санитизации → навигация между
   главами.

7. **Выбор перевода** — UI при `MultipleTranslationsError`, проброс `branch_id` дальше
   в чтение/экспорт/скачивание. Коммиты: обработка исключения → UI выбора → проброс
   `branch_id` в зависимые эндпоинты (чтение, экспорт, download_title).

8. **Экспорт одной главы и нескольких выбранных глав** — форматы динамически из
   `EXPORTERS`, отдача файла, удаление временного файла после ответа. Коммиты: список
   форматов из `EXPORTERS` → экспорт одной главы → экспорт нескольких выбранных одним
   файлом → очистка временных файлов.

9. **Скачивание тома/томов** — `get_volume()`/`get_volumes()` + экспорт целиком.
   Коммиты: эндпоинт → UI кнопки на странице тайтла.

10. **Скачивание всего тайтла с прогрессом** — background job (in-memory очередь), эндпоинт
    старта и эндпоинт статуса (polling/SSE), обработка `MultipleTitleTranslationsError`
    (выбор по каждой неоднозначной главе или общий `branch_id`/`translation_index` на
    тайтл), UI «глава N из M». Коммиты: job-очередь → эндпоинт старта → эндпоинт статуса →
    обработка `MultipleTitleTranslationsError` → UI прогресса.

11. **История недавно открытых тайтлов** (опционально, не MVP, если не закрыто в PR 4).

12. **Полировка error UX** — сквозная проверка всех путей на `RateLimitError`,
    `AuthRequiredError` и остальные сообщения из таблицы, финальная проверка, что нигде не
    протекает трейсбек в ответ пользователю.

13. **Аккаунты и авторизация приложения** — своя регистрация/логин/логаут (email + пароль,
    хэш через passlib/bcrypt), серверные сессии, middleware защиты роутов, схема БД
    приложения (`app/db/`, таблица `users`). Инфраструктурный PR второй волны — нужен
    PR 14, 17, 18. Коммиты: схема БД и миграции → модель `User` → регистрация → логин/
    логаут/сессии → middleware защиты роутов → тесты.

14. **Персональная библиотека — вкладка «Читаю»** — добавление тайтла в личную библиотеку
    по ссылке (переиспользует поиск из PR 4), сохранение прогресса чтения per user
    (последняя открытая глава при чтении из PR 6), вкладка «Читаю» в разделе «Библиотека»
    по умолчанию показывает только эти тайтлы. Требует PR 13. Коммиты: модель
    `LibraryEntry` → эндпоинт добавить/убрать из библиотеки → запись прогресса чтения при
    открытии главы → вкладка «Читаю» в UI.

15. **Каталог «Все тайтлы» (заблокирован до релиза в SDK)** — вторая вкладка «Библиотеки»:
    полный список тайтлов RanobeLib с постепенной подгрузкой, поиском и фильтрами. Блокер:
    в SDK (проверено на актуальной версии `0.5.0`) нет метода листинга каталога —
    `RanobeLib` умеет работать только с одним тайтлом по ссылке, это же прямо исключено в
    «Что явно не делать» ниже. Автор этого репозитория — он же мейнтейнер
    `ranobelib-python-sdk`, поэтому здесь это не «issue и жди стороннего человека», а
    «спланировать и выпустить метод в самом SDK» — но правило то же: сначала метод
    листинга релизится в SDK (issue в
    https://github.com/LLIJIYAPNIK/ranobelib-python-sdk/issues как обычно — для трекинга и
    чтобы предполагаемая сигнатура была зафиксирована и обсуждаема, а не придумана на ходу
    в этом репозитории), потом этот PR его использует. Никакого скрапинга каталога в обход
    SDK даже как временный воркэраунд — при наличии контроля над SDK «сделать по-нормальному
    в SDK» дешевле, чем городить обходной путь в вебе. PR стартует только после выхода новой
    версии SDK на PyPI с этим методом. Коммиты (после релиза в SDK): апдейт зависимости на
    новую версию SDK → клиент листинга в `services/` → постепенная подгрузка (infinite
    scroll/пагинация) → поиск → фильтры.

16. **Карточки с обложками** — тайтлы (недавние из PR 4/11, личная библиотека из PR 14)
    отображаются карточками с `Title.cover` вместо списка/текста. Ничего нового от SDK не
    требуется — поле `cover` уже есть в модели `Title` начиная как минимум с `0.5.0`.
    Коммиты: компонент карточки в шаблонах → недавние тайтлы карточками → личная
    библиотека карточками.

17. **Раздел «Загрузки»** — список текущих job'ов (скачивание тайтла и сразу следующий за
    ним экспорт как один пользовательский процесс), статус, оставшееся время (см. решение
    выше — считается по тикам `on_chapter`). История завершённых загрузок пишется в БД
    приложения per user (нужен PR 13); сама job-очередь остаётся in-memory, как решено для
    MVP. Коммиты: модель истории загрузок → расчёт ETA по тикам прогресса → эндпоинт
    статуса списком → UI раздела «Загрузки».

18. **Раздел «Активность»** — за сегодня: прочитанные главы по тайтлам, карточки тайтлов,
    которые читались (переиспользует PR 16), активное время чтения, текущие загрузки
    (переиспользует PR 17), тайтлы, скачанные сегодня. Активное время чтения — лёгкий
    heartbeat с фронта, пока открыта страница главы и вкладка активна; чистый web-слой, без
    доменной логики про ранобэ. Требует PR 13, 14, 17. Коммиты: модель событий активности
    (чтение главы, heartbeat, скачивание) → heartbeat-эндпоинт → агрегация «за сегодня» →
    UI раздела.

19. **Адаптация под мобильные устройства** — доводка и QA всех экранов (включая новые из
    PR 14–18) под мобильные брейкпоинты, уже заданные в импортированном `Concept 3 - Media
    Collection Dashboard.dc.html`; не переизобретать вёрстку, только донастройка того, чего
    не было в исходной концепции (например карточки «Активности»). Логично делать последним
    в волне. Коммиты: аудит всех экранов на мобильных брейкпоинтах → фиксы по списку →
    регресс-тест на десктопных брейкпоинтах (не сломать).

20. **Обрыв загрузки длинных тайтлов** — при большом количестве глав/томов загрузка тайтла
    (страница тайтла и/или `download_title()`) обрывается раньше конца. Сначала
    диагностировать источник: RanobeLib (сайт отдаёт неполные данные/рвёт соединение на
    длинных тайтлах) или сам SDK (лимит/таймаут/пагинация внутри `get_table_of_contents()`/
    `download_title()`). Если проблема на стороне RanobeLib — искать обход на веб-слое
    (например повторные точечные докачки недостающего), не трогая контракт SDK. Если
    ограничивает сам SDK — разобраться в причине; в обоих случаях после диагностики
    практически неизбежно нужен issue в SDK
    (https://github.com/LLIJIYAPNIK/ranobelib-python-sdk/issues), т.к. либо баг в самом
    SDK, либо SDK нужно научить обходить особенность RanobeLib. Коммиты: диагностика и
    воспроизведение на конкретном длинном тайтле → issue в репозитории SDK с описанием и
    примером → (если применимо) временный воркэраунд на веб-слое с TODO-комментарием со
    ссылкой на issue.

21. **Убрать дублирующую надпись на главной** — на главной странице уже есть placeholder
    прямо в поле ввода ссылки на тайтл (например `https://ranobelib.me/ru/book/6712--high-
    school-dxd-novel` или короткий идентификатор `6712--high-school-dxd-novel`); отдельная
    надпись над/рядом с полем дублирует эту подсказку — убрать её. Коммиты: убрать надпись →
    проверить, что placeholder в поле остаётся понятным без неё.

22. **Заблокированные экраны «Загрузок» и «Активности» для неавторизованных** — на странице
    «Библиотека» уже есть экран-заглушка для неавторизованных пользователей; такой же гейт
    нужен для «Загрузок» (PR 17) и «Активности» (PR 18), так как оба раздела персональные и
    без авторизации (PR 13) не имеют смысла. Коммиты: вынести существующий locked-screen
    «Библиотеки» в переиспользуемый компонент/шаблон (если ещё не выделен) → применить на
    «Загрузках» → применить на «Активности».

23. **Иконки вместо текста в сайдбаре** — заменить текстовые пункты меню сайдбара на SVG-
    иконки; конкретные иконки подобрать самостоятельно по смыслу каждого раздела (Главная,
    Библиотека, Загрузки, Активность и т.д.), сохранив доступность — `aria-label`/`title`
    или tooltip с названием раздела, чтобы пункт меню не терял смысл без подписи. Коммиты:
    подбор/добавление SVG-иконок для каждого пункта → замена текстовых пунктов на иконки →
    добавление tooltip/aria-label.

24. **Баг: страница «Библиотека» не подгружает тайтлы на экранах 2К+** — на широких/высоких
    экранах бесконечная подгрузка не срабатывает сама по себе, помогает только ручное
    изменение масштаба страницы — похоже на триггер подгрузки, завязанный на scroll-событие,
    которое не происходит, если список изначально помещается на экране без скролла.
    Исправить триггер так, чтобы он проверял необходимость подгрузки и при монтировании/
    resize, а не только по факту скролла. Коммиты: диагностика текущего триггера подгрузки →
    фикс триггера (проверка при монтировании и resize) → ручная проверка на 2K/4K и на
    обычных экранах (не сломать).

25. **Названия новелл на русском, если есть** — там, где сейчас выводится оригинальное/
    английское название тайтла, использовать русское название, если оно доступно. Сначала
    проверить (по Golden rule выше), отдаёт ли SDK такое поле в актуальной версии на PyPI
    (`CHANGELOG.md`, страница пакета) — если да, переключить рендер на него с фолбэком на
    оригинальное название при отсутствии перевода; если такого поля в SDK нет — завести issue
    в https://github.com/LLIJIYAPNIK/ranobelib-python-sdk/issues, не парсить/угадывать
    русское название на веб-слое в обход SDK. Коммиты: проверка наличия поля в актуальной
    версии SDK → (если есть) рендер русского названия с фолбэком → (если нет) issue в
    репозитории SDK.

26. **Убрать декоративный квадрат в шапке сайдбара** — элемент в самом верху сайдбара не
    несёт функции и не отображает никакой информации — убрать. Коммит: удалить элемент,
    поправить отступы/вёрстку шапки сайдбара без него.

27. **Прогресс чтения** — отображать прогресс чтения тайтла в соответствии с дизайном
    (Concept 3), опираясь на уже сохраняемый прогресс (последняя открытая глава, PR 14).
    Коммиты: расчёт процента прогресса (прочитано глав / всего глав) → компонент прогресс-
    бара по дизайну → вывод на карточках тайтлов и на странице тайтла.

28. **Навигация внизу страницы главы** — сейчас навигация «пред./след. глава» (PR 6) есть
    только вверху страницы главы, добавить такой же блок внизу, после текста главы.
    Коммит: продублировать блок навигации внизу страницы, переиспользуя разметку/логику
    верхнего блока.

29. **Примерный вес новеллы на странице тайтла** — показывать приблизительный объём/размер
    новеллы. Сначала проверить (по Golden rule выше), отдаёт ли SDK готовое поле или оценку
    размера; если нет — рассчитать лёгкую оценку на веб-слое на основе уже полученных данных
    (например суммарный объём текста доступных глав), не занимаясь парсингом контента заново
    в обход SDK. Коммиты: проверка наличия готового поля/оценки в SDK → реализация расчёта
    (на стороне SDK, если добавлено, либо лёгкая оценка на веб-слое) → вывод значения на
    странице тайтла.

30. **Меню настроек чтения — типографика и ширина области чтения** — модальное/выезжающее
    меню настроек на странице главы: шрифт, межстрочный интервал, другие типографические
    параметры, а также ширина контентной колонки (особенно полезно на больших/широких
    экранах, чтобы не скроллить взгляд из края в край). Настройки сохраняются между главами
    и сессиями: `localStorage` для неавторизованных, при наличии авторизации (PR 13) —
    опционально в БД приложения per user. Коммиты: UI меню настроек → применение шрифта/
    межстрочного интервала к контенту главы → контрол ширины области чтения (CSS-переменная
    на контейнер) → сохранение всех настроек (`localStorage`/БД).

31. **Жанры на странице тайтла как ссылки в каталог с фильтром** — жанры должны быть
    кликабельными ссылками, ведущими в каталог тайтлов с уже применённым фильтром по этому
    жанру. Блокер: наследует блокер PR 15 («Все тайтлы») — без каталога с листингом и
    фильтрами (который сам заблокирован до появления метода листинга в SDK) вести ссылку
    некуда, поэтому этот пункт можно закрыть только после PR 15. Коммиты (после PR 15):
    жанр как ссылка на странице тайтла → поддержка параметра фильтра по жанру в каталоге →
    проверка сквозного перехода «жанр на странице тайтла → каталог с фильтром».

32. **Ограничить название тайтла в карточках тремя строками** — в карточках тайтлов
    (недавние/личная библиотека из PR 16, в перспективе каталог «Все тайтлы» из PR 15)
    длинные названия сейчас ничем не ограничены и ломают вёрстку карточки. Ограничить
    видимую высоту тремя строками, после лимита — многоточие (CSS line-clamp:
    `-webkit-line-clamp: 3` + `overflow: hidden`/`text-overflow: ellipsis`; это чисто
    визуальное ограничение на веб-слое, не обрезка данных). Полное название остаётся
    доступным — `title`-атрибут/тултип на карточке и целиком на странице тайтла. Коммиты:
    CSS-ограничение в 3 строки с многоточием на карточках → `title`/тултип с полным
    названием → проверка на очень длинных и однословных названиях.

33. **Настройки чтения — отдельная страница вместо модалки, иконка в сайдбаре** —
    пересматривает решение PR 30: меню настроек чтения было модальным/выезжающим и
    открывалось только со страницы главы; вынести его на отдельную страницу (например
    `/settings`), доступную напрямую из сайдбара, а не только из контекста конкретной
    главы. Добавляет к иконкам сайдбара из PR 23 ещё один пункт — «Настройки», с той же
    практикой `aria-label`/tooltip. Коммиты: роут и шаблон страницы настроек → иконка
    «Настройки» в сайдбаре со ссылкой на страницу → перенос содержимого модалки (PR 30) на
    страницу, модалка убирается → проверка, что настройки по-прежнему применяются к
    странице главы после переноса.

34. **Настройки чтения — выбор шрифта и слайдеры с живым превью** — доработка страницы
    настроек из PR 33 (делать после неё): (1) добавить выбор шрифта из нескольких
    вариантов вместо текущего фиксированного; (2) заменить контрол размера шрифта на
    слайдер (range) с живым превью — пример текста на странице сразу меняется при
    движении ползунка, без промежуточного сохранения/перезагрузки; (3) то же самое
    (слайдер + живое превью) для ширины области чтения. Чисто типографика/веб-слой, SDK не
    касается. Коммиты: выбор шрифта (список + применение `font-family`) → слайдер размера
    шрифта с live-превью → слайдер ширины области чтения с live-превью → сохранение новых
    параметров в `localStorage`/БД (расширение существующего сохранения из PR 30).

35. **Автодобавление тайтла в библиотеку при открытии главы** — сейчас (PR 14) тайтл
    попадает в личную библиотеку только по явному нажатию кнопки «Добавить». Открытие
    любой главы тайтла (страница чтения, PR 6) авторизованным пользователем (PR 13) должно
    само создавать запись в библиотеке, если её ещё нет — так же, как если бы пользователь
    нажал кнопку. Кнопка добавления остаётся (нужна, например, чтобы добавить тайтл со
    страницы тайтла/карточки, не открывая главу), но перестаёт быть единственным способом.
    Коммиты: автосоздание `LibraryEntry` при открытии главы, если записи ещё нет →
    идемпотентность (не дублировать при повторных открытиях) → UI сразу отражает
    добавленное состояние кнопки после открытия главы без доп. действий пользователя.

36. **Чекбокс «Запомнить меня» на форме входа** — сейчас сессионная cookie логина (PR 13)
    живёт фиксированное время, из-за чего приходится логиниться заново чаще, чем хотелось
    бы. Добавить чекбокс на форме логина: включён — увеличенный срок жизни cookie (значение
    вынести в конфиг, не хардкодить), выключен — текущее поведение без изменений. Коммиты:
    чекбокс в шаблоне формы логина → разный `max_age` сессионной cookie в зависимости от
    чекбокса → тест на оба варианта (обычный логин / «запомнить меня»).

37. **Плавающая панель экспорта выбранных глав** — на странице тайтла (PR 5/8) панель с
    кнопкой «Скачать выбранные» стоит перед списком глав; при выборе чекбоксов на длинном
    оглавлении панель уходит из вида после скролла вниз, и кнопку приходится специально
    искать. Сделать панель прилипающей (`position: sticky`/`fixed`) и плавно выезжающей
    (CSS-transition) при появлении первого выбранного чекбокса, а не только статично видимой
    сверху. Коммиты: sticky-позиционирование панели → transition появления/скрытия при
    изменении выбора → проверка на длинных оглавлениях (много томов/глав).

38. **Фильтрация каталога по жанру чекбоксами (заблокирован до релиза в SDK)** — в каталоге
    «Все тайтлы» (PR 15) добавить блок чекбоксов для фильтрации по жанру (мультивыбор), в
    дополнение к одиночной ссылке-жанру со страницы тайтла (PR 31). Сама фильтрация уже
    поддержана SDK: `Catalog.list_titles(genres=[...])` (проверено на актуальной версии
    `0.6.1`) принимает список id жанров с семантикой «И» (тайтл должен иметь все указанные),
    новый SDK-метод для самой фильтрации не нужен. Блокер: по докстрингу того же метода в
    SDK нет публичного способа получить сам список всех жанров (id → название) без
    авторизации, а без него нечем заполнить чекбоксы. Сначала issue в
    https://github.com/LLIJIYAPNIK/ranobelib-python-sdk/issues с запросом на метод листинга
    жанров (например `Catalog.list_genres()`) — не хардкодить список жанров на веб-слое в
    обход SDK. PR стартует только после появления такого метода в SDK. Коммиты (после
    релиза в SDK): апдейт зависимости на новую версию SDK → клиент списка жанров в
    `services/` → чекбоксы жанров в UI каталога → проброс выбранных id в `genres=[...]` →
    синхронизация с infinite-scroll (PR 15/24) и сохранение выбора при подгрузке страниц.

39. **Бургер-меню сайдбара — разворачивать с названиями пунктов** — сайдбар сейчас всегда в
    свёрнутом виде (только иконки, PR 23/26). Добавить кнопку-бургер, разворачивающую
    сайдбар в широкое состояние с подписями рядом с иконками (текстовые названия из PR 23,
    сейчас доступные только через tooltip/`aria-label`); состояние (свёрнуто/развёрнуто)
    сохраняется между переходами (`localStorage`). Коммиты: кнопка-бургер и состояние
    развёрнуто/свёрнуто → раскладка сайдбара с подписями в развёрнутом состоянии →
    сохранение состояния в `localStorage` → плавный переход между состояниями (transition
    ширины).

40. **Увеличить область тапа у иконок сайдбара на мобильных** — на мобильных (PR 19)
    кликабельная область иконок сайдбара сейчас ограничена самой иконкой, из-за чего сложно
    попасть тапом. Увеличить `padding`/hit-area иконок до рекомендуемого минимума (например
    44×44px) на мобильных брейкпоинтах, не увеличивая визуальный размер самой иконки.
    Коммит: увеличить hit-area иконок сайдбара на мобильных брейкпоинтах (padding вокруг
    SVG, без изменения видимого размера), проверить, что соседние пункты не перекрываются.

41. **Баг: подпись «Жанр: …» в каталоге прижата к краю экрана** — на странице каталога с
    активным фильтром по жанру (`content__hint` в `catalog.html`) подпись «Жанр: Боевик ·
    Сбросить фильтр» рендерится без отступа от края экрана на мобильных. Добавить
    горизонтальный отступ (padding/margin), консистентный с остальным контентом страницы.
    Коммит: отступ для `.content__hint` от края экрана на мобильных брейкпоинтах, проверка,
    что не сломалась десктопная вёрстка.

42. **Анимация иконок сайдбара** — добавить hover/active-переходы для иконок сайдбара
    (PR 23/26/39): плавное изменение цвета/масштаба при наведении и подсветка активного
    пункта, вместо мгновенного переключения состояния. Коммиты: hover-transition для иконок
    → анимация активного/выбранного пункта → проверка на touch-устройствах (без залипающего
    hover-состояния).

43. **Индикатор загрузки при подсчёте веса новеллы** — расчёт примерного объёма/веса
    новеллы на странице тайтла (PR 29) может занимать заметное время; сейчас страница просто
    ждёт результат без обратной связи. Показать спиннер и статусные подписи по ходу
    вычисления (например «Загружаем главы…», «Подсчитываем размер…»), а не пустое ожидание.
    Если расчёт синхронный в текущей реализации PR 29 — потребуется вынести его в
    асинхронный запрос с индикатором (аналогично поллингу прогресса в PR 10/17), не блокируя
    отрисовку остальной страницы тайтла. Коммиты: асинхронная загрузка расчёта веса (если
    ещё не асинхронная) → компонент спиннера со сменой статусных подписей → подстановка
    итогового значения по готовности.

44. **Hover-эффекты для кнопок** — добавить единообразные hover-переходы (цвет/тень/
    масштаб) для кнопок по всему приложению там, где их сейчас нет или они мгновенные без
    transition. Коммиты: аудит кнопок без hover-transition → добавление единого
    hover-стиля/анимации → проверка на touch-устройствах.

45. **Более современный стиль и анимация выпадающих меню** — текущие выпадающие меню
    (например выбор перевода PR 7, выбор формата экспорта PR 8, сортировка каталога)
    оформлены максимально просто и без анимации открытия/закрытия. Добавить анимацию
    появления (fade/slide) и обновить визуальный стиль (тени, скругления, hover-состояния
    пунктов) в соответствии с общим дизайном (Concept 3). Коммиты: обновлённый визуальный
    стиль выпадающих меню → анимация открытия/закрытия → проверка на всех местах
    использования (выбор перевода, формат экспорта, сортировка).

46. **Анимация перехода между разделами сайдбара на мобильных** — на мобильных (PR 19)
    переключение раздела через сайдбар происходит мгновенно, без перехода. Добавить лёгкую
    анимацию перехода между разделами (например fade/slide контента) при переключении
    пункта сайдбара. Технически удобнее делать после PR 39 (разворачиваемый сайдбар), чтобы
    анимировать оба состояния сайдбара согласованно. Коммиты: анимация перехода контента при
    смене раздела на мобильных → проверка, что не увеличивает заметно время до отрисовки
    контента → регресс-тест на десктопе (анимация не проявляется там, где не нужна).

47. **Анимация перехода между вкладками «Читаю» и «Все тайтлы»** — переключение вкладок
    каталога/библиотеки (PR 14/15, `_library_tabs.html`) сейчас происходит без анимации.
    Добавить плавный переход (например fade/slide содержимого и/или анимацию индикатора
    активной вкладки) при переключении между «Читаю» и «Все тайтлы». Коммиты: анимация
    индикатора активной вкладки → анимация смены содержимого при переключении → проверка на
    мобильных и десктопных брейкпоинтах.

48. **Высота выделения активного пункта в развёрнутом сайдбаре** — `.sidebar__link`
    (PR 23/39) не задаёт вертикальный padding даже в развёрнутом состоянии
    (`.sidebar--expanded .sidebar__link` сейчас только `padding: 0 22px` — только
    горизонтальный), из-за чего фон активного пункта (`.sidebar__link--active`) плотно
    обтягивает текст по высоте и выглядит непропорционально сжатым на фоне остальной строки
    меню. Коммит: добавить вертикальный padding для `.sidebar__link` в развёрнутом
    состоянии, проверить на всех пунктах сайдбара.

49. **Выделение активного пункта в свёрнутом сайдбаре не центрировано на иконке** — в
    свёрнутом состоянии `.sidebar__link` тоже не имеет padding, поэтому фон
    `.sidebar__link--active` обтягивает саму иконку впритык и выглядит смещённым/неровным
    вместо аккуратного центрированного квадрата вокруг иконки. Дать `.sidebar__link`
    одинаковый квадратный hit-box в свёрнутом состоянии с иконкой по центру и
    `border-radius` на этом боксе (тот же принцип hit-area, что уже увеличена на мобильных
    в PR 40). Коммит: padding/фиксированный размер и центрирование для `.sidebar__link` в
    свёрнутом состоянии, проверить на десктопе и на свёрнутом мобильном виде.

50. **Баг: файл скачивания тайтла не доставляется, если уйти со страницы загрузки** —
    диагностика: доставка готового файла (`GET
    /titles/{slug}/download/{job_id}/file`, `app/api/downloads.py`) сейчас триггерится
    только клиентским скриптом (`download-status.js`), который поллит статус и делает
    `window.location.href = fileUrl` при `status == "done"` — но этот скрипт живёт только
    на странице конкретной задачи (`/titles/{slug}/download/{job_id}`). Сам background job
    не завязан на открытую вкладку и докачивает главы независимо (см. `app/jobs/download.py`),
    но если пользователь ушёл с этой страницы до завершения — или вообще не открывал её,
    просто нажал «Скачать» и продолжил пользоваться сайтом — результат никуда не
    доставляется. Дополнительно `/file` удаляет временный файл сразу после первой отдачи
    (`BackgroundTask(os.remove, ...)`), так что даже случайный повторный заход на страницу
    задачи позже не поможет, если единственный шанс был пропущен. Нужен доставочный
    механизм, не завязанный на нахождение на странице конкретной задачи: например лёгкий
    глобальный поллинг статуса активных задач пользователя с любой страницы сайта (баннер/
    тост «Файл готов — скачать», переиспользуя список из PR 17), и не удалять файл экспорта,
    пока пользователь его не забрал явным кликом (или пока не истечёт TTL, если так и не
    забрал). Коммиты: подтверждение сценария (диагностика выше) → глобальный поллинг
    статуса активных задач пользователя, не привязанный к странице конкретной задачи →
    баннер/тост с ссылкой на готовый файл, видимый на любой странице сайта → пересмотр
    удаления временного файла экспорта (по явному клику скачивания или по TTL, а не при
    первой попытке отдачи) → проверка сценария «ушёл со страницы задачи до завершения».

51. **Плавающая панель экспорта всё ещё не видна при скролле (доработка PR 37)** — PR 37
    сделал панель `position: sticky` (`.toc__export-panel`, `top: 16px`), но на практике при
    длинном оглавлении и активной прокрутке панель всё равно пропадает из вида (см.
    скриншот с выбранными главами и отсутствующей кнопкой). `sticky` держит элемент только
    в пределах его containing block и может визуально теряться или перекрываться другими
    элементами (например мобильной фиксированной шапкой аккаунта из PR 46 — `z-index: 40`
    против `z-index: 5` у панели). Сделать панель полноценно `position: fixed` (как
    настоящий navbar/toolbar), зафиксированной по высоте относительно окна просмотра и с
    учётом других fixed-элементов (мобильная шапка аккаунта и нижний таб-бар, PR 46), а не
    полагаться на позицию в потоке документа. Коммиты: диагностика причины пропадания на
    реальном длинном оглавлении → замена `position: sticky` на `position: fixed` с учётом
    других fixed-элементов → пересчёт отступов контента, чтобы фиксированная панель не
    перекрывала первые чекбоксы → регресс-тест на десктопе и мобильном, коротких и длинных
    оглавлениях.

52. **Меню управления чтением — показывать при скролле вверх** — сейчас блок навигации
    главы (`_chapter_nav.html`, PR 6/28) виден только в потоке страницы вверху/внизу; чтобы
    вызвать его посреди чтения, нужно долистать до самого верха. Сделать так, чтобы любой
    скролл вверх (даже небольшой) показывал меню управления как выезжающую сверху панель с
    плавной анимацией, а скролл вниз — прятал её обратно, чтобы не мешать чтению. Коммиты:
    детектор направления скролла → фиксированная накладывающаяся панель меню поверх
    контента → анимация выезда/скрытия → проверка, что не конфликтует с меню настроек
    чтения (PR 30/33) и навигацией снизу страницы (PR 28).

53. **Стилизовать чекбоксы жанров под дизайн сайта** — чекбоксы фильтра по жанру в каталоге
    (PR 38) сейчас используют вид браузера по умолчанию и визуально выбиваются из тёмной
    темы сайта. Кастомизировать внешний вид (свой чек-маркер, цвета в акцентной палитре
    сайта, состояния hover/focus/checked) как у остальных интерактивных элементов
    интерфейса. Коммиты: кастомные стили чекбоксов (checked/hover/focus-visible) →
    проверка доступности (фокус клавиатурой, контраст) → проверка на всём списке жанров
    (много чекбоксов подряд).

54. **Кастомизировать выпадающие списки (доработка PR 45)** — PR 45 анимировал открытие/
    закрытие контейнера выпадающих меню, но там, где список вариантов — нативный `<select>`
    (например сортировка каталога), сам список опций браузер не даёт полноценно
    стилизовать через CSS ни в одном движке, поэтому анимация/стиль контейнера проблему не
    решает — пункты списка остаются дефолтными (см. скриншот). Заменить `<select>` там, где
    нужен кастомный вид списка, на свой JS-компонент (кастомный listbox на `<button>` +
    список с `role="listbox"`), сохранив доступность (клавиатурная навигация, `aria-*`
    атрибуты) и рабочий `<select>` как no-JS фоллбэк. Коммиты: кастомный dropdown-компонент
    (разметка + `aria`-атрибуты) → стилизация пунктов списка (hover/выбранный) →
    интеграция вместо `<select>` сортировки каталога → интеграция в остальных выпадающих
    меню сайта → no-JS фоллбэк на нативный `<select>`.

55. **Стилизовать вертикальный скроллбар под дизайн сайта** — сейчас скроллбар везде в
    системном/браузерном виде, выбивается из тёмной темы. Кастомизировать через
    `scrollbar-color`/`scrollbar-width` (Firefox) и `::-webkit-scrollbar*` (Chromium/Safari)
    в акцентной палитре сайта. Коммиты: кастомные стили скроллбара (оба API) → проверка на
    всех прокручиваемых областях (страница целиком, сайдбар, длинные списки/оглавления).

56. **Счётчик активных загрузок на иконке «Загрузки» в сайдбаре** — рядом с иконкой
    «Загрузки» (PR 23) показывать кружок с числом активных задач скачивания, по аналогии с
    уведомлениями на телефоне. Переиспользовать уже существующий поллинг активных задач
    (`GET /downloads/status`, PR 17/50), не заводить для бейджа отдельный опрос. Коммиты:
    бейдж-компонент на иконке сайдбара → обновление числа из уже существующего поллинга →
    скрытие бейджа, когда активных задач нет.

57. **Удаление записей из истории загрузок** — на странице «Загрузки» (PR 17,
    `_download_history.html`/таблица `download_history`) сейчас нет способа убрать
    отдельную запись из «Истории». Добавить удаление по записи. Коммиты: DELETE-эндпоинт по
    id записи (с проверкой, что запись принадлежит текущему user_id) → кнопка удаления в
    списке истории → подтверждение действия → тест на попытку удалить чужую запись (403/404).

58. **Кнопка «Скачать файл» в истории загрузок, если уведомление о готовности закрыто** —
    сейчас (`_download_history.html`) запись истории не даёт скачать файл повторно: если
    пользователь закрыл тост готовности (PR 50, кнопка `download-ready__dismiss` — она
    только прячет уведомление в текущей сессии через `sessionStorage`, не отменяет job и не
    удаляет файл, который живёт до TTL или явного скачивания, см. `list_ready_jobs_for_user()`
    в `app/jobs/store.py`), у пользователя не остаётся способа вернуться к файлу, кроме как
    знать точный URL задачи. Добавить кнопку «Скачать» в конце каждой строки истории (после
    даты), видимую только пока файл ещё доступен (тот же критерий, что уже использует
    `list_ready_jobs_for_user()`). Для этого `download_history` должна научиться хранить
    `job_id` — сейчас таблица его не сохраняет (миграция добавляет колонку,
    `record_download()` начинает принимать и писать `job.id`). Коммиты: миграция + запись
    `job_id` в `download_history` → проверка на сервере, доступен ли файл этой записи (жив
    ли job в `list_ready_jobs_for_user()`) → кнопка «Скачать» в истории при доступном файле →
    проверка сценария «закрыл тост → зашёл в Загрузки → всё равно скачал файл».

59. **Чекбоксы выбора глав на странице тайтла — обновлённый дизайн (доработка PR 53)** —
    чекбоксы выбора глав для экспорта на странице тайтла (`.toc__chapter-checkbox`, PR 5/8)
    всё ещё в виде браузера по умолчанию, как жанры каталога были до PR 53 — привести к
    тому же кастомному стилю (чек-маркер/цвета/hover/focus), что PR 53 уже сделал для
    чекбоксов жанров. Коммиты: применить кастомные стили чекбоксов из PR 53 к
    `.toc__chapter-checkbox` → проверка при массовом выборе глав → проверка доступности
    (фокус клавиатурой).

60. **Баг: лаги при разворачивании сайдбара (бургер) в каталоге** — диагностика: `.sidebar`
    анимирует `width`/`flex-basis` при переключении (PR 39), а `.title-card-grid` в
    каталоге использует `grid-template-columns: repeat(auto-fill, minmax(140px, 1fr))` — на
    каждый кадр этой анимации браузер пересчитывает раскладку grid для всех карточек, а на
    странице каталога их особенно много из-за бесконечной подгрузки (PR 15/24), которая
    ничего не убирает из DOM по мере скролла — отсюда заметные лаги именно на этой странице,
    а не по всему сайту. Коммиты: подтверждение причины (профилирование анимации при
    большом числе подгруженных карточек) → фикс (например ограничить пересчёт layout грида
    во время transition сайдбара и/или не держать в DOM карточки далеко за пределами
    вьюпорта) → проверка на каталоге с сотнями подгруженных карточек и на других страницах
    с сайдбаром (не сломать).

61. **Разделы в меню настроек чтения (доработка PR 33/34)** — сейчас `settings.html`
    выводит все поля одним плоским списком (шрифт, размер, интервал, ширина) без
    группировки — не масштабируется по мере появления новых настроек (см. следующие
    пункты). Разбить страницу на именованные разделы (например «Типографика» — шрифт/
    размер/интервал/ширина, и новый раздел «Чтение с помощью тапа» под PR 63–65 ниже), с
    заголовками секций, сохранив текущее поведение (мгновенное применение, `localStorage`).
    Коммиты: разметка секций с заголовками на странице настроек → перенос существующих
    полей типографики в свою секцию → пустая секция «Чтение с помощью тапа» под следующие
    пункты → проверка, что применение и сохранение настроек не сломалось.

62. **Чтение с помощью тапа — сама механика** — новый способ чтения главы: вместо всего
    текста главы, показанного сразу, абзацы раскрываются по одному при тапе — уже
    показанные абзацы остаются на месте (растущая лента, а не слайд-шоу), новый добавляется
    под ними при следующем тапе. Тап-зона — вся видимая область экрана, а не только сам
    текст/абзац (не нужно целиться в текст). После раскрытия нового абзаца страница сама
    плавно доскролливает к нему — не заставлять пользователя листать руками. Когда раскрыт
    последний абзац главы, следующий тап переводит на следующую главу (та же навигация, что
    и обычная кнопка «след. глава», PR 6/7); если это последняя глава тайтла — следующий тап
    уводит на страницу тайтла с уведомлением «Тайтл прочитан» (прогресс чтения уже
    отслеживается, PR 14/27, раз глава дочитана до конца). Реализуется поверх уже
    отрендеренного `chapter.content` (`.reader-content`, PR 6) — чистый JS/CSS на веб-слое,
    доменной логики или SDK не касается. Коммиты: разбор `.reader-content` на абзацы и их
    скрытие/раскрытие по одному → обработчик тапа по всей видимой области, а не только по
    тексту → автоскролл к новому абзацу при раскрытии → переход на следующую главу по тапу
    после последнего абзаца → переход на страницу тайтла с уведомлением о прочтении по тапу
    на последнем абзаце последней главы → сохранение позиции (какой абзац уже раскрыт) при
    уходе со страницы и возврате → проверка на главах с не-`<p>` контентом (изображения/
    цитаты и т.п. из sanitized HTML SDK — не ломать раскрытие).

63. **Настройка «Чтение с помощью тапа» в настройках чтения** — добавить переключатель в
    разделе «Чтение с помощью тапа» страницы настроек (созданном в PR 61), включающий/
    выключающий механику из PR 62 (по умолчанию выключено — обычное чтение остаётся
    поведением по умолчанию). Хранится и применяется так же, как остальные настройки чтения
    (`localStorage`, `readerSettings`, PR 30). Коммиты: чекбокс/тумблер в разделе настроек →
    сохранение значения в `readerSettings` → включение/выключение механики PR 62 на
    странице главы по этому значению.

64. **Кастомизация чтения тапом — стиль выделения абзаца** — под настройкой из PR 63
    добавить выбор внешнего вида раскрытого абзаца: (1) «в стиле переписки» — абзац
    оформлен как сообщение в чате (прямоугольная плашка-фон под текстом абзаца и метка
    времени рядом/под ней, как время отправки сообщения — момент раскрытия абзаца тапом, не
    хранится между сессиями, чисто декоративно); (2) «просто прямоугольник» — тот же
    прямоугольный фон под абзацем, но без чат-плашки и метки времени. Оба стиля — варианты
    одного select'а в разделе «Чтение с помощью тапа». Коммиты: стилизация «плашка + время»
    → стилизация «просто прямоугольник» → выбор стиля в настройках (раздел PR 61/63) →
    проверка обоих стилей на разных экранах/брейкпоинтах.

65. **Кастомизация чтения тапом — анимация появления абзаца** — добавить выбор анимации
    появления при раскрытии абзаца (PR 62): без анимации, снизу вверх, слева направо с
    лёгким рывком (overshoot), проявление (fade), печатная машинка (посимвольное появление —
    тематически перекликается со стилем переписки из PR 64), фокусировка (blur → чёткое
    изображение). Выбор — там же, в разделе «Чтение с помощью тапа» настроек. Коммиты:
    CSS/JS-реализация каждой анимации → выбор анимации в настройках → сохранение выбора в
    `readerSettings` → проверка производительности на длинных главах (много абзацев подряд).

66. **Полноэкранный просмотр изображений в главе** — картинки внутри `chapter.content`
    (`.reader-content img`, PR 6) сейчас открываются только инлайн в потоке текста. Добавить
    полноэкранный просмотр по клику на изображение: зум (приближение/отдаление), кнопка
    скачать изображение, стрелки вперёд/назад для перехода между всеми изображениями главы
    без закрытия просмотра. Недоступно в режиме чтения тапом (PR 62–65) — там сама модель
    взаимодействия (тап по всей области экрана раскрывает следующий абзац) конфликтует с
    открытием лайтбокса по тапу на изображении, поэтому в этом режиме изображения остаются
    обычными инлайн-картинками. Коммиты: компонент полноэкранного просмотра (открытие/
    закрытие) → зум → кнопка скачивания изображения → стрелки навигации между изображениями
    главы → отключение лайтбокса при включённом чтении тапом (переключатель из PR 63) →
    проверка на главах без изображений/с одним изображением (стрелки скрыты/неактивны).

67. **Баг: страница «Загрузки» падает и «ломает сайт» — общее sqlite3-соединение не
    потокобезопасно** — трейсбек: `sqlite3.InterfaceError: bad parameter or other API
    misuse` в `get_user_by_id()` (`app/db/users.py:44`), вызванном из `get_current_user()`
    (`app/auth/dependencies.py`). Причина: `app/db/connection.py` держит один
    процесс-широкий `sqlite3.Connection` (`check_same_thread=False`), общий для всех
    запросов — но `check_same_thread=False` только отключает проверку «тот же поток», а не
    делает сам объект соединения безопасным для одновременного использования из разных
    потоков. FastAPI выполняет синхронные зависимости (включая `get_current_user`, который
    дёргается почти на каждой странице) в пуле потоков (`run_in_threadpool`), а страница
    «Загрузки» вдобавок опрашивает `/downloads/status` и `/downloads/ready` каждые
    несколько секунд (PR 17/50/56) — при пересечении запросов два потока одновременно
    вызывают `.execute()` на одном и том же объекте `Connection`, что и роняет его с этой
    ошибкой. Дальше это не поломка одного запроса: то же самое соединение использует вообще
    любой запрос к БД приложения, так что один такой конфликт может испортить состояние
    соединения для всех последующих запросов — отсюда «просто ломает сайт», а не только
    один эндпоинт. Исправить на потокобезопасный доступ к БД — например соединение
    per-thread (`threading.local()`, каждый поток открывает свой `sqlite3.Connection` к
    тому же файлу) вместо одного общего объекта, либо сериализовать доступ к общему
    соединению через `threading.Lock`. Коммиты: воспроизведение под параллельной нагрузкой
    (несколько поллеров одновременно) → переход на потокобезопасный доступ (per-thread
    соединения или lock) в `app/db/connection.py` → проверка, что все места, использующие
    `get_connection()`, по-прежнему работают корректно → нагрузочная проверка страницы
    «Загрузки» с активным поллингом в нескольких вкладках.

68. **Прогресс чтения на карточках «Недавние»** — «Недавние» на главной (`index.html`,
    `app/recent_titles.py`) — анонимная cookie-история, без прогресса чтения; при этом
    прогресс уже считается для «Читаю» (PR 27, `reading_progress_percent()`) через
    `LibraryEntry` в БД приложения. Показать тот же прогресс-бар (`.reading-progress`, тот
    же `{% call title_card(...) %}`-паттерн, что уже используется в `library.html`) и на
    карточках «Недавние» — но только когда есть с чем сопоставить: авторизованный
    пользователь (PR 13) и совпадающая запись в его `LibraryEntry` с `last_read_volume`
    (PR 14/35 — тайтл из «Недавних» уже мог попасть туда автоматически при открытии главы).
    Для анонимного посетителя или тайтла, который открыли только на странице описания,
    ничего не считать и не хранить отдельно — это не повод заводить параллельное хранилище
    прогресса поверх уже существующего в БД. Коммиты: подтягивание `progress_percent` для
    каждой записи «Недавних», у которых есть текущий пользователь и совпадающий
    `LibraryEntry` → рендер `.reading-progress` на карточке «Недавние» → проверка для
    анонимного посетителя и для тайтла без прогресса (ничего лишнего не показывается).

69. **Скрыть тайтл из «Недавних»** — сейчас (`app/recent_titles.py`) убрать запись из
    cookie-истории можно только естественным вытеснением (хранится не более
    `_MAX_ENTRIES` = 8, самые старые вытесняются новыми) — нет способа убрать конкретный
    тайтл вручную, например если пользователь открыл только страницу описания и не хочет
    видеть его в «Недавних». Добавить кнопку «×» на карточке (тот же паттерн, что уже есть
    у dismiss-тостов, PR 50) и эндпоинт, переписывающий cookie без этой записи (симметрично
    уже существующей `remember()`). Коммиты: функция вроде `forget()` в
    `app/recent_titles.py`, переписывающая cookie без выбранной записи → POST-эндпоинт,
    вызывающий её и обновляющий cookie на ответе → кнопка «×» на карточке в `index.html` →
    удаление карточки из DOM без перезагрузки страницы.

70. **Баг: строка истории загрузок не исчезает без перезагрузки страницы после удаления** —
    диагностика: `download-history-delete.js` (PR 57) делает `row.remove()` после
    успешного `DELETE`, но `row = button.closest("[data-entry-id]")` — а сама кнопка
    удаления тоже несёт свой собственный `data-entry-id` (`_download_history.html`, атрибуты
    `data-role="delete-history-entry" data-entry-id="{{ entry.id }}"` на самой кнопке),
    поэтому `closest()` останавливается на самой кнопке, а не доходит до родительского
    `<li data-entry-id="...">`. В итоге из DOM убирается только сама кнопка «×», а вся
    остальная строка (ссылка, формат, статус, дата) остаётся на экране — хотя запись на
    сервере уже реально удалена (см. `DELETE /downloads/history/{id}`), и следующая
    перезагрузка страницы честно её уже не показывает. Коммит: заменить селектор на что-то,
    однозначно указывающее на строку, а не на саму кнопку (например
    `button.closest("li[data-entry-id]")` или `.closest(".downloads-history__item")`),
    проверить, что строка пропадает сразу после удаления без перезагрузки.

71. **Баг: фиксированная плашка аккаунта перекрывает контент на мобильных** —
    диагностика: `.sidebar__account` в мобильном виде (`position: fixed; top: 0`, PR 46)
    компенсируется фиксированным `padding-top: 48px` на `.main` — если реальная высота
    плашки на каком-то экране/длине email отличается от этих зашитых 48px (перенос строки,
    другой масштаб шрифта у пользователя и т.п.), контент под ней перекрывается. Сначала
    подтвердить сценарий (при каких условиях высота плашки расходится с 48px, см.
    скриншот), затем не полагаться на захардкоженную высоту с обеих сторон — например
    замерять реальную высоту плашки и прокидывать её в CSS-переменную (`ResizeObserver`),
    либо гарантировать, что плашка всегда ровно одной высоты (запретить перенос строки,
    обрезать email многоточием, как уже сделано в свёрнутом сайдбаре). Коммиты: диагностика
    условий расхождения → фикс (переменная высота через `ResizeObserver` или гарантированная
    фиксированная высота плашки) → проверка на длинных email и разных мобильных
    брейкпоинтах.

72. **Не закреплять меню управления чтением (PR 52) на мобильных** — `.reader-scroll-nav`
    сейчас выезжает по скроллу вверх на любом брейкпоинте, включая мобильный; на мобильных
    телефонах, где страница чтения и так уже упрощена (сайдбар и плашка аккаунта скрыты,
    `body.reader-page`, PR 46), эта дополнительная всплывающая панель поверх текста больше
    мешает чтению, чем помогает — навигация по главам и так есть в потоке страницы (PR 6/28).
    Отключить поведение PR 52 на мобильном брейкпоинте, оставив его как есть на
    десктопе/планшете. Коммит: медиа-запрос, отключающий скролл-детектор/показ
    `.reader-scroll-nav` на мобильных брейкпоинтах, проверка, что обычная навигация по
    главам (верх/низ страницы) остаётся доступной.

73. **Тап-зона в режиме чтения тапом — весь экран, а не только текст (доработка PR 62)** —
    сейчас (`tap-to-read.js`) обработчик клика повешен на `content`
    (`[data-role="chapter"]`, он же `.reader-content`) — у этого элемента
    `max-width: var(--reader-width); margin: 0 auto`, то есть по ширине это только колонка
    текста, а по высоте — ровно высота уже раскрытых абзацев; тап мимо этой области (сбоку
    от узкой колонки, либо ниже последнего показанного абзаца, до того как контент займёт
    весь экран) не долетает до обработчика. Повесить обработчик на всю видимую область
    чтения (например `.reader-page`/`document`, за вычетом навигации и интерактивных
    элементов), а не на сам `.reader-content`. Коммиты: перенос обработчика тапа на
    полноэкранную область → исключение кликов по ссылкам/навигации/настройкам из
    срабатывания → проверка на разных высотах экрана и коротких главах (когда раскрытого
    текста меньше высоты вьюпорта).

74. **Полноэкранный просмотр изображений в режиме чтения тапом (пересматривает решение
    PR 66)** — PR 66 сознательно отключил `image-lightbox.js` целиком при включённом
    чтении тапом (`isTapToReadEnabled()` в начале файла), чтобы тап по изображению не
    конфликтовал с тапом «показать следующий абзац». Теперь это нужно совместить: тап
    именно по изображению должен открывать полноэкранный просмотр (зум/скачивание/стрелки,
    как и в обычном режиме), а тап по любой другой области — раскрывать следующий абзац,
    как в PR 73. Требует координации между `tap-to-read.js` и `image-lightbox.js` —
    например `image-lightbox.js` больше не выключает себя целиком в режиме тапа, а
    полноэкранный обработчик тапа из PR 73 явно игнорирует клики по `<img>` внутри
    `.reader-content` (оставляя их собственному обработчику `image-lightbox.js`). Коммиты:
    убрать полное отключение `image-lightbox.js` в режиме тапа → исключить `<img>` из
    полноэкранного обработчика тапа (PR 73) → проверка, что тап по изображению открывает
    лайтбокс, а не раскрывает абзац, и наоборот.

75. **Баг: тап после последнего абзаца не переводит на следующую главу/тайтл (доработка
    PR 62)** — по исходному замыслу PR 62 (см. пункт 62) тап после раскрытия последнего
    абзаца главы должен вести на следующую главу, а на последней главе тайтла — на страницу
    тайтла с уведомлением «Тайтл прочитан»; в реальной реализации (`tap-to-read.js`)
    обработчик клика делает `if (revealedCount >= wraps.length) return;` — то есть после
    последнего абзаца тап буквально ничего не делает. Реализовать переход, как было
    задумано изначально. Коммиты: переход на следующую главу по тапу после последнего
    абзаца (если она есть) → редирект на страницу тайтла с уведомлением «Тайтл прочитан» на
    последней главе тайтла → проверка на тайтле с одной главой (сразу и последний абзац, и
    последняя глава).

76. **Баг: автоскролл при раскрытии абзаца не работает в режиме чтения тапом (доработка
    PR 62)** — по исходному замыслу страница должна сама плавно доскролливать к только что
    раскрытому абзацу; в `tap-to-read.js` функция `reveal()` добавляет/показывает новый
    `wrap`, но нигде не вызывает `scrollIntoView()` (или аналог) — пользователю приходится
    долистывать вручную, как жаловались до PR 62. Добавить автоскролл к новому абзацу сразу
    после его раскрытия. Коммит: `scrollIntoView({ behavior: "smooth", ... })` (или ручная
    анимация скролла) на вновь раскрытый `wrap` в конце `reveal()`, проверка, что не
    конфликтует со сохранением позиции чтения при возврате на страницу (PR 62).

77. **Тест скорости чтения** — новый подраздел «Скорость чтения» внутри раздела «Чтение с
    помощью тапа» настроек (PR 61): готовый образец текста, слова обёрнуты в кликабельные
    элементы. Пользователь запускает тест, 60 секунд читает текст в комфортном темпе, по
    истечении таймера кликает по слову, на котором остановился — количество прочитанных
    слов за ровно одну минуту и есть скорость чтения (слов/мин). Результат сохраняется в
    `readerSettings` (например `readingSpeedWpm`) тем же способом, что и остальные настройки
    чтения (PR 30). Коммиты: образец текста и разметка кликабельных слов → таймер на 60
    секунд и запуск/остановка теста → обработка клика по слову и подсчёт слов/мин →
    сохранение результата в `readerSettings` и отображение текущего значения в разделе
    настроек.

78. **Ручной ввод скорости чтения без прохождения теста** — в том же подразделе «Скорость
    чтения» — числовое поле/слайдер, позволяющий задать или поправить скорость чтения
    напрямую, независимо от PR 77: тест — не единственный способ заполнить
    `readingSpeedWpm`, ручной ввод должен работать и для тех, кто тест вообще не проходил.
    Оба способа пишут в один и тот же ключ настроек, поэтому значение всегда согласовано
    между тестом и ручным полем. Коммиты: числовое поле/слайдер ручного ввода скорости →
    разумные границы вводимого значения (валидация) → синхронизация отображения между
    результатом теста (PR 77) и ручным полем при загрузке страницы настроек.

79. **Темп появления текста в режиме тапа по скорости чтения (5 анимаций)** — новая
    настройка «Темп появления текста» в разделе «Чтение с помощью тапа», использующая
    `readingSpeedWpm` (PR 77/78): вместо мгновенного раскрытия абзаца при тапе (текущее
    поведение PR 62, поверх которого работают декоративные анимации PR 65) текст
    появляется растянуто во времени, на столько, сколько реально заняло бы его прочтение на
    заданной скорости. Пять вариантов темпа на выбор: «слово за словом» (текст проявляется
    по одному слову с интервалом, вычисленным из WPM), «печатная машинка по скорости»
    (WPM-версия уже существующего «печатная машинка» из PR 65 — вместо текущего
    fixed-budget тайминга интервал между символами считается из реальной скорости),
    «построчно» (раскрытие по строкам), «плавное выделение» (бегущая подсветка вдоль
    текста абзаца в темпе чтения), «растворение слов» (слова одно за другим проявляются из
    расфокуса). Значение по умолчанию — «мгновенно» (нынешнее поведение, без завязки на
    скорость). Коммиты: реализация каждой из 5 темп-анимаций, привязанных к
    `readingSpeedWpm` → селектор темпа появления в разделе «Чтение с помощью тапа» →
    интеграция с `tap-to-read.js` (раскрытие абзаца растягивается на реальное время чтения
    вместо мгновенного показа) → проверка на очень длинных и очень коротких абзацах (не
    растягивать до неудобных пауз и не мелькать слишком быстро).

80. **Стилизовать фокус текстовых полей по всему сайту** — `.search-input` (поиск на
    главной/в каталоге) и `.auth-form__input` (логин/регистрация) сейчас вообще не задают
    `:focus`/`:focus-visible` — подсветка при фокусе дефолтная браузерная (белая обводка),
    выбивается из тёмной темы, хотя у остальных интерактивных элементов сайта
    (`.toc__export-format`, `.toc__chapter-checkbox`, чекбоксы жанров) кастомный
    `:focus-visible` уже есть (`box-shadow` в акцентном teal). Применить тот же паттерн ко
    всем текстовым инпутам сайта, а не только на странице авторизации. Коммиты:
    `:focus-visible` для `.search-input` → `:focus-visible` для `.auth-form__input` →
    проверка остальных текстовых полей сайта, чтобы нигде не осталось дефолтной обводки.

81. **Стилизовать чекбокс на странице авторизации** — чекбокс «Запомнить меня»
    (PR 36, `login.html`, `.auth-form__field--checkbox input`) — обычный
    `<input type="checkbox">` без кастомного вида, хотя кастомные стили чекбоксов уже есть
    (жанры каталога — PR 53, чекбоксы выбора глав — PR 59). Применить тот же стиль здесь.
    Коммит: кастомные стили чекбоксов из PR 53 на `.auth-form__field--checkbox input`,
    проверить состояния (checked/hover/focus-visible).

82. **Стилизовать и анимировать ссылки на страницах аутентификации** — ссылка
    «Зарегистрироваться» (`login.html`, внутри `.content__hint`) и аналогичные ссылки на
    других auth-экранах сейчас без hover-эффекта и анимации перехода, в отличие от
    остального интерфейса (PR 44/45). Добавить hover-переход (цвет/подчёркивание) и лёгкую
    анимацию. Коммиты: hover-стиль для ссылок auth-страниц → transition/анимация →
    проверка на всех auth-экранах (вход, регистрация).

83. **Прогресс чтения главы в оглавлении — режим тапа** — сейчас прогресс (PR 27) считается
    только на уровне тайтла (прочитано глав / всего), а внутри конкретной начатой, но не
    дочитанной главы прогресса не видно. В режиме чтения тапом (PR 62) это почти бесплатно:
    `tap-to-read.js` уже хранит `revealedCount`/`wraps.length` в localStorage
    (`tapToReadProgress:`) на главу — на странице тайтла в оглавлении показать
    мини-прогрессбар/процент для такой главы, а для полностью раскрытой
    (`revealedCount >= wraps.length`) — галочку, стилизованную под дизайн сайта, вместо
    прогрессбара. Коммиты: чтение `tapToReadProgress` для каждой главы оглавления при
    рендере `toc` → компонент мини-прогрессбара/процента на строке главы → компонент
    галочки для полностью прочитанных глав → проверка на главах без сохранённого прогресса
    (ничего не показывается).

84. **Прогресс чтения главы в оглавлении — обычный режим (доработка PR 83)** — то же самое
    для обычного (не тап) чтения сложнее: готового счётчика вроде `revealedCount` нет,
    нужно самому отслеживать, докуда долистал пользователь — например по видимости
    последнего/самого нижнего показанного абзаца во вьюпорте (`IntersectionObserver` на
    абзацах `.reader-content`) или по проценту скролла страницы. Сохранять как процент
    (0–100) в том же по смыслу localStorage-ключе, что и PR 83 (единое хранилище прогресса
    главы независимо от режима чтения — не заводить два несовместимых представления одного
    и того же прогресса), переиспользуя компонент прогрессбара/галочки из PR 83. Коммиты:
    отслеживание прогресса по абзацам через `IntersectionObserver` в обычном режиме чтения
    → пересчёт в общий процент и запись в то же хранилище, что PR 83 → переиспользование
    прогрессбара/галочки из PR 83 для обычного режима → проверка, что переключение между
    режимами (тап/обычный) на одной и той же главе не путает и не сбрасывает прогресс.

85. **Панель «Фильтры» в каталоге (жанры + страна)** — сейчас чекбоксы жанров (PR 38) живут
    в компактном сворачиваемом блоке `.catalog-genres` над сеткой карточек. Собрать
    полноценную панель «Фильтры» по референсу интернет-магазинов — постоянно видимая
    колонка справа от сетки карточек (не сворачиваемый попап), с разделами по типу фильтра.
    Раздел «Жанры» переносит уже существующие чекбоксы (PR 38 — `Catalog.list_titles
    (genres=[...])` и `list_genres()` оба уже есть в SDK `0.7.0`, тот блокер снят) в новую
    панель без изменения логики фильтрации. Раздел «Страна» заблокирован: ни в модели
    `Title`, ни в `Catalog.list_titles()` в SDK `0.7.0` нет ничего похожего на страну/
    регион происхождения тайтла — ни поля, ни параметра фильтра, ни метода листинга стран.
    Один issue в https://github.com/LLIJIYAPNIK/ranobelib-python-sdk/issues с запросом на
    добавление страны (поле в `Title` + параметр фильтра в `list_titles()` + метод листинга
    вроде `list_countries()`, по аналогии с `list_genres()`) — не хардкодить список стран на
    веб-слое. Раздел «Страна» стартует только после релиза в SDK. Коммиты: вёрстка правой
    колонки «Фильтры» (десктоп/мобайл) → перенос раздела «Жанры» в новую панель → issue в
    репозитории SDK на страну → (после релиза в SDK) раздел «Страна» тем же паттерном, что
    и «Жанры».

86. **Фильтрация каталога по тегам (`title-hero__badges`, заблокирован до релиза в SDK)** —
    на странице тайтла второй блок с классом `title-hero__badges` — теги (`title.tags`,
    отдельная от жанров модель), сейчас просто `<span class="badge badge--muted">`, не
    ссылки. Сделать их кликабельными, ведущими в каталог с фильтром по тегу — тот же
    паттерн, что жанры (PR 31). Блокер: в SDK `0.7.0` `Catalog.list_titles()` принимает
    `genres`/`status`/`query`/`sort`, но не тег — фильтровать по тегам через API нечем, а
    выкачивать весь каталог и фильтровать вручную на веб-слое в обход API прямо запрещено
    (см. «Что явно не делать»). Issue в
    https://github.com/LLIJIYAPNIK/ranobelib-python-sdk/issues с запросом параметра `tags`
    в `list_titles()` (по аналогии с `genres`, семантика «И»/«ИЛИ» уточняется в самом
    issue). PR стартует только после релиза в SDK. Коммиты (после релиза в SDK): апдейт
    зависимости → тег как ссылка на странице тайтла → поддержка параметра фильтра по тегу в
    каталоге → проверка сквозного перехода «тег на странице тайтла → каталог с фильтром».

87. **Авторы/художники/переводчики на странице тайтла — ссылки в каталог с фильтром (без
    чекбоксов в панели, заблокирован до релиза в SDK)** — `title.authors`/`title.artists`/
    `title.teams` (`title.html`) сейчас выводятся как текст через запятую. Сделать каждое
    имя ссылкой в каталог, предфильтрованный по этому человеку/команде — тот же паттерн,
    что жанры (PR 31), но без чекбоксов в панели «Фильтры» (PR 85): людей и команд перевода
    слишком много для списка чекбоксов, только прямые ссылки с конкретной страницы тайтла.
    Блокер: в SDK `0.7.0` у `Title` уже есть `authors`/`artists`/`teams` (каждый с
    `id`/`slug_url`), но `Catalog.list_titles()` не принимает ни один из них как фильтр.
    Issue в https://github.com/LLIJIYAPNIK/ranobelib-python-sdk/issues с запросом
    параметров фильтра по автору/художнику/переводческой команде в `list_titles()`
    (например `author_id`/`artist_id`/`team_id`) — одна фича, один issue, три похожих
    параметра в одной и той же сигнатуре метода. PR стартует только после релиза в SDK.
    Коммиты (после релиза в SDK): апдейт зависимости → авторы/художники/переводы как
    ссылки на странице тайтла → поддержка параметров фильтра в каталоге → проверка, что эти
    фильтры не показываются как чекбоксы в панели «Фильтры» (PR 85).

88. **Профиль вместо email/«Выйти» в шапке** — сейчас `.sidebar__account` (PR 13, десктопный
    сайдбар и мобильная фиксированная шапка из PR 46) показывает email текстом и отдельную
    кнопку «Выйти». Заменить на одну аватарку пользователя — без загруженного изображения
    (загрузка аватара не в скоупе этой фичи) показывать инициалы, вычисленные из email (пока
    никнейма ещё нет, см. PR 90 — после него инициалы переключаются на никнейм). Сама кнопка
    «Выйти» пока никуда не ведёт, кроме как через новое меню (PR 93) — на время между PR 88 и
    PR 93 достаточно, чтобы аватар был кликабельной ссылкой на `/settings`. Коммиты: компонент
    аватарки (изображение/инициалы) → замена `.sidebar__account` на аватарку на десктопе и
    мобильной шапке → вычисление инициалов из email → временная ссылка на `/settings` до
    готовности PR 93.

89. **Настройки — вкладки вместо одной страницы с секциями** — пересматривает PR 61: там
    настройки чтения сгруппированы в секции на одной странице `/settings`. По референсу
    (скриншот) сделать полноценные вкладки в стиле GitHub Settings — левое меню разделов,
    каждый раздел на своём под-роуте (например `/settings/reading` для типографики и чтения
    тапом, переносит содержимое PR 61 без потерь), плюс пустые пока разделы «Аккаунт» и
    «Безопасность» под следующие пункты. Активный пункт левого меню подсвечивается по
    текущему под-роуту. Коммиты: разметка левого меню разделов → под-роуты для каждого
    раздела → перенос содержимого PR 61 в раздел «Чтение» без потери функциональности →
    пустые разделы «Аккаунт» и «Безопасность» → мобильная адаптация левого меню.

90. **Поля аккаунта — никнейм, email, bio** — в разделе «Аккаунт» (PR 89): миграция БД
    (`migrations/0006_...sql`) добавляет `nickname`/`bio` в таблицу `users` (email уже есть,
    просто становится редактируемым через форму, с той же проверкой уникальности, что и при
    регистрации). Никнейм не обязан быть уникальным — в приложении нет ни поиска пользователей
    по нему, ни входа по никнейму, только email (см. `app/auth/dependencies.py`), так что
    уникальность здесь была бы искусственным ограничением без функциональной причины. После
    этого пункта аватар (PR 88) переключает источник инициалов на никнейм, если он заполнен.
    Коммиты: миграция → форма редактирования (никнейм/email/bio) → проверка уникальности email
    при смене → переключение инициалов аватара на никнейм, если он есть → уведомление об
    успешном сохранении.

91. **Раздел «Безопасность» — смена пароля** — в разделе «Безопасность» (PR 89): форма с
    текущим паролем, новым паролем и подтверждением. Текущий пароль проверяется через уже
    существующий `verify_password()` (`app/auth/passwords.py`), новый хэшируется через
    `hash_password()` — обе функции уже есть и используются при регистрации/логине, здесь
    просто переиспользуются, а не дублируются. Неверный текущий пароль — ошибка формы, а не
    молчаливый отказ. Остальные активные сессии пользователя (PR 13) этот пункт не трогает —
    отдельная тема, не в скоупе. Коммиты: форма смены пароля → проверка текущего пароля →
    запись нового хэша → тест на неверный текущий пароль и на несовпадение подтверждения.

92. **Страница профиля (только чтение)** — новая страница `/profile`, отдельная от
    `/settings`: аватар (PR 88), никнейм или email (если никнейма нет), bio, дата регистрации
    (`users.created_at`) — всё только на чтение, без форм редактирования. Кнопка
    «Редактировать» ведёт в `/settings/account` (PR 89/90), где эти же поля реально меняются.
    Осмысленна только после PR 90 — до него никнейма/bio просто нет, показывать нечего кроме
    email и даты регистрации. Коммиты: роут и шаблон страницы профиля → рендер аватара/
    никнейма/bio/даты регистрации → кнопка «Редактировать» на `/settings/account` → пустые
    состояния для пользователя без никнейма/bio.

93. **Выпадающее меню профиля** — по клику на аватарку (PR 88) показывать меню с пунктами
    «Профиль» (PR 92), «Читаю» (уже существующая вкладка библиотеки, PR 14), «Настройки»
    (PR 89), «Выйти» (существующая форма логаута, PR 13) — по референсу (скриншот 1), но
    открывается вниз-вверх от аватарки, а не вверх-вниз, как в референсе, потому что сама
    аватарка теперь внизу сайдбара/в верхней мобильной шапке, а не наверху экрана. Коммиты:
    компонент выпадающего меню (открытие/закрытие по клику на аватар, закрытие по клику вне
    меню) → четыре пункта меню со ссылками на существующие к этому моменту страницы →
    позиционирование снизу вверх на десктопе и в мобильной шапке → анимация открытия/закрытия
    (тот же паттерн, что PR 45/54).

94. **Стилизовать кнопки числового поля (доработка PR 78)** — `.reading-speed-test__manual-field
    input` (ручной ввод скорости чтения, `type="number"`) использует нативные стрелки
    браузера вверх/вниз — их нельзя полноценно стилизовать через CSS ни в одном движке (тот
    же ограничение, что уже решалось для `<select>` в PR 54). Скрыть нативные стрелки
    (`-webkit-appearance: none` / `-moz-appearance: textfield`) и добавить свои кнопки
    +/− рядом с полем, в стиле сайта. Коммиты: скрыть нативные стрелки → кастомные кнопки
    +/− (разметка и стили) → JS-обработчик увеличения/уменьшения значения с уважением к
    `min`/`max`/`step` поля → проверка ручного ввода с клавиатуры (кастомные кнопки не
    единственный способ менять значение).

95. **Алфавитный порядок разделов настроек (доработка PR 89)** — сейчас пункты левого меню
    настроек идут в порядке добавления фичи (Чтение, Аккаунт, Безопасность), а не по алфавиту.
    Отсортировать пункты `.settings-nav` по названию раздела (Аккаунт, Безопасность, Чтение).
    Коммит: отсортировать пункты меню настроек по алфавиту, проверить, что активный пункт и
    под-роуты по-прежнему совпадают после переупорядочивания.

96. **Возможность изменить аватар пользователя** — сейчас (PR 88) аватар — это всегда буквы
    (инициалы из никнейма/email), загрузка своего изображения сознательно не входила в скоуп
    той фичи. Добавить загрузку: поле выбора файла в разделе «Аккаунт» (PR 90), ограничение
    по типу (jpg/png/webp) и размеру файла, хранение на диске в отдельной директории
    приложения (не в SDK `cache_dir` — это данные пользователя, а не кэш ranobelib.me, см.
    «Архитектура»), путь к файлу — новая колонка в `users` (миграция `0007_...sql`). На
    персистентном volume при деплое — та же оговорка, что уже сделана для `cache_dir` в
    конфигурации, иначе аватары пропадают при каждом рестарте контейнера. Везде, где сейчас
    рендерятся инициалы (сайдбар PR 88, меню профиля PR 93, страница профиля PR 92) —
    показывать загруженное изображение вместо них, если оно есть. Коммиты: миграция и
    колонка `avatar_path` → форма загрузки в разделе «Аккаунт» → валидация типа/размера файла
    → сохранение на диск с учётом персистентного volume → рендер аватара вместо инициалов
    везде, где он уже показывается.

97. **Баг: меню профиля не отображается поверх остального интерфейса** — диагностика:
    `.profile-menu__panel` (PR 93) — `position: absolute` внутри `.profile-menu`, которая, в
    свою очередь, лежит внутри `.sidebar` — а у `.sidebar` `overflow-x: hidden; overflow-y:
    auto` (нужно для собственного скролла длинного списка пунктов). Абсолютно
    спозиционированный потомок всё равно обрезается по границам этого предка с `overflow`,
    так что меню визуально «зажато» рамками сайдбара вместо того, чтобы всплывать поверх
    всей страницы (см. скриншоты) — `z-index: 40` тут не помогает, потому что `overflow`
    обрезает содержимое до всякой отрисовки поверх других слоёв. Исправить переносом самой
    панели меню в `<body>` при открытии (JS «портал» — переместить/отрисовать элемент вне
    `.sidebar`), с `position: fixed` и координатами, вычисленными по `getBoundingClientRect()`
    кнопки-аватарки, а не полагаться на CSS-позиционирование внутри обрезающего предка.
    Коммиты: подтверждение причины (overflow предка) → перенос панели меню в `<body>` при
    открытии → расчёт позиции через `getBoundingClientRect()` аватарки → проверка на
    десктопе и мобильной шапке, что меню больше не обрезается и не съезжает при скролле/ресайзе.

98. **Кнопка «Фильтры» вместо всегда закреплённой панели (пересматривает PR 85)** — сейчас
    `.catalog-filters` — это `position: sticky` колонка, всегда видимая рядом с сеткой
    карточек. Убрать постоянное закрепление: добавить кнопку «Фильтры» в конце строки поиска
    (после кнопки «Искать», выровнена по правому краю формы), по клику открывающую панель
    фильтров, повторный клик — закрывает; внутри самой панели добавить кнопку-крестик для
    закрытия. Коммиты: кнопка «Фильтры» в конце `.search-form`, выровненная по правому краю →
    JS-переключение видимости `.catalog-filters` по клику → кнопка-крестик внутри панели →
    закрытие повторным кликом по кнопке «Фильтры» → анимация появления/скрытия (тот же
    паттерн, что PR 45/54).

99. **Сворачиваемые секции внутри «Фильтры» (доработка PR 85/98)** — внутри открытой панели
    (PR 98) разделы «Жанры» и «Страна» сейчас всегда развёрнуты целиком — длинный список
    жанров (полсотни с лишним пунктов) занимает много места, даже если посетителя интересует
    только страна. Сделать заголовок каждого раздела кликабельным аккордеоном, сворачивающим/
    разворачивающим список опций под ним, независимо для каждого раздела. Коммиты: аккордеон
    для `.catalog-filters__section` (заголовок как переключатель) → анимация сворачивания/
    разворачивания → сохранение состояния свёрнутости между открытиями панели (например
    `localStorage`) → проверка, что выбранные чекбоксы в свёрнутом разделе не сбрасываются.

100. **Страна в фильтрах — чекбоксы с семантикой «ИЛИ» (заблокирован до релиза в SDK)** —
     сейчас страна в панели «Фильтры» (PR 85) — это радиокнопки (выбор ровно одной страны).
     Сделать чекбоксами (мультивыбор), с семантикой «ИЛИ» — показывать тайтлы из любой из
     отмеченных стран, а не требовать все сразу (что для страны происхождения и не имело бы
     смысла — тайтл принадлежит ровно одной стране, в отличие от жанров). Блокер: в SDK
     `0.8.0` `Catalog.list_titles(country=...)` принимает только один id
     (`country: int | None`), докстринг прямо говорит «a title only has one country of
     origin, unlike genres» — метода/параметра для списка стран нет. Issue в
     https://github.com/LLIJIYAPNIK/ranobelib-python-sdk/issues с запросом расширить
     `country` до списка (например `countries: list[int]`) с семантикой «ИЛИ»/`IN`, что для
     единственной страны на тайтл — единственная осмысленная семантика (в отличие от жанров,
     где пришлось явно фиксировать «И»). PR стартует только после релиза в SDK. Коммиты
     (после релиза в SDK): апдейт зависимости → чекбоксы вместо радиокнопок для страны →
     проброс списка выбранных стран в `countries=[...]` → синхронизация с infinite-scroll и
     сохранение выбора при подгрузке страниц.

101. **Показывать поисковую строку каталога при скролле вверх** — тот же паттерн, что уже
     есть для чтения (`.reader-scroll-nav`, PR 52), но для другого блока: на каталоге
     (`.header` с полем поиска, сортировкой и кнопкой «Фильтры» из PR 98) при скролле вниз
     строка поиска уходит из вида (см. скриншот), а чтобы её вызвать заново — приходится
     скроллить обратно до самого верха страницы. Показывать `.header` как выезжающую сверху
     панель при любом скролле вверх, прятать при скролле вниз. Коммиты: детектор направления
     скролла для каталога (переиспользовать логику PR 52, если возможно, в отдельном модуле) →
     фиксированная накладывающаяся панель `.header` поверх сетки карточек → анимация выезда/
     скрытия → проверка, что не конфликтует с кнопкой «Фильтры» (PR 98) и её панелью.

102. **Кнопка «наверх» в каталоге** — при длинной подгруженной сетке карточек (бесконечная
     подгрузка, PR 15/24) нет быстрого способа вернуться к началу списка, кроме долгого
     скролла руками. Добавить плавающую кнопку внизу справа, появляющуюся после скролла на
     какую-то глубину, прокручивающую страницу наверх плавно по клику. Коммиты: кнопка
     «наверх» (появление/скрытие по глубине скролла) → плавный скролл к началу страницы по
     клику → проверка, что не перекрывает кнопку «наверх» из других уже существующих
     плавающих элементов (например тосты готовности загрузки, PR 50).

103. **Мобильные фильтры — полноэкранная модалка (доработка PR 85/98)** — сейчас на мобильных
     `.catalog-filters` просто встаёт в поток страницы над сеткой карточек (`order: -1`).
     По кнопке «Фильтры» (PR 98) вместо этого открывать полноэкранную модалку, выезжающую
     анимацией справа налево, с собственной кнопкой закрытия (крестик, тот же элемент, что
     уже добавлен в PR 98). Референс «нормальных» фильтров интернет-магазина (скриншот) —
     общая идея (заголовок «Фильтры», список разделов, чекбоксы), не полный перенос
     вложенного поиска по фильтрам — это отдельная, более крупная фича, не в скоупе этого
     пункта. Коммиты: полноэкранная модалка вместо `order: -1` на мобильных брейкпоинтах →
     анимация выезда справа налево при открытии → кнопка закрытия внутри модалки → проверка,
     что применение фильтров закрывает модалку и обновляет список карточек.

104. **Мобильные настройки — сначала список разделов (доработка PR 89)** — сейчас на
     мобильных `.settings-nav` — горизонтальный скроллящийся ряд из трёх вкладок над
     содержимым раздела. Сделать так, как устроены системные настройки на смартфонах: сначала
     список разделов (Аккаунт, Безопасность, Чтение — в алфавитном порядке, PR 95), при тапе
     на раздел — переход в содержимое именно этого раздела (с кнопкой/стрелкой «назад» к
     списку разделов). Только на мобильных брейкпоинтах — десктопное поведение (левое меню +
     контент рядом, PR 89) не меняется. Коммиты: список разделов как отдельный экран на
     мобильных → переход в содержимое раздела по тапу → кнопка «назад» к списку разделов →
     проверка, что десктопная раскладка не изменилась.

105. **Никнейм в форме регистрации** — сейчас (`register.html`) форма регистрации просит
     только email и пароль — никнейм можно задать только потом, в настройках (PR 90).
     Добавить необязательное поле «Никнейм» прямо в форму регистрации, использующее ту же
     колонку `users.nickname`, что и PR 90 (без новой миграции). Коммиты: поле «Никнейм» в
     форме регистрации → проброс значения в `create_user()` → проверка регистрации без
     никнейма (поле по-прежнему необязательно, как и в настройках).

106. **Предложить сменить аватар сразу после регистрации** — сейчас успешная регистрация
     (`app/api/auth.py`) сразу редиректит на `/`. Вставить между регистрацией и главной
     страницей один экран: «Хотите добавить аватар?» с загрузкой файла (переиспользует
     форму/эндпоинт из PR 96) и кнопкой «Пропустить», ведущей на `/`. Пользователь уже
     залогинен на этом экране (сессия создаётся при регистрации, до этого шага), так что
     сама загрузка технически ничем не отличается от загрузки аватара из настроек. Коммиты:
     редирект после регистрации на новый промежуточный экран вместо `/` → переиспользование
     формы загрузки аватара из PR 96 на этом экране → кнопка «Пропустить» → редирект на `/`
     после загрузки или пропуска.

107. **Баг: кнопки шага скорости чтения залипают подсвеченными после клика** —
     `.reading-speed-test__stepper-btn` (`app.css`, ручной ввод скорости чтения, PR 94)
     задаёт `:focus-visible` (тёмный контур в акцентном teal) отдельно от
     `@media (hover: hover) { :hover }` — но клик мышью по `<button>` в части браузеров
     всё равно оставляет на нём `:focus-visible` до следующего клика в другое место, из-за
     чего кнопка «−»/«+» выглядит «залипшей» подсвеченной сразу после нажатия, а не только
     при реальной клавиатурной навигации, для которой `:focus-visible` и задумывался.
     Обработчики кликов (`reading-speed-test.js`, `manualDecrement`/`manualIncrement`) сейчас
     не снимают фокус после срабатывания. Снять фокус с кнопки сразу после клика (`blur()`
     в обработчике), сохранив сам `:focus-visible`-контур для клавиатурной навигации (Tab).
     Коммиты: `blur()` кнопки после клика в обработчиках `nudge()` → проверка, что
     клавиатурный фокус (Tab) по-прежнему подсвечивается → проверка на обеих кнопках
     («−» и «+») и повторных быстрых кликах.

108. **Баг: сайдбар заново анимируется при каждом переходе между страницами** — сайдбар
     (`sidebar-toggle.js`, PR 39) хранит состояние «развёрнут/свёрнут» в `localStorage`, но
     приложение — server-rendered многостраничное (Jinja2, без SPA-навигации), поэтому
     `.sidebar` при каждом переходе между страницами (настройки → библиотека → каталог)
     рендерится в HTML заново в свёрнутом состоянии по умолчанию, и только после загрузки
     страницы `sidebar-toggle.js` считывает `localStorage` и добавляет класс
     `sidebar--expanded` — а `.sidebar { transition: width 0.2s ease, flex-basis 0.2s ease; }`
     заставляет эту смену класса каждый раз визуально проигрывать анимацию расширения,
     даже когда сохранённое состояние уже было «развёрнуто». Убрать лишнее проигрывание:
     например применять сохранённое состояние синхронно до первой отрисовки (инлайновый
     скрипт в `<head>`/начале `<body>`, до рендера `.sidebar`, аналогично тому, как решается
     мерцание темы в других SSR-приложениях), либо не навешивать transition на первый рендер
     страницы и включать его только на самом переключении по клику. Коммиты: диагностика
     (подтверждение выше) → синхронное применение сохранённого состояния до отрисовки
     сайдбара (инлайн-скрипт) или подавление transition на первой отрисовке → проверка, что
     анимация по-прежнему проигрывается при живом клике по бургеру, но не при переходах
     между страницами.

109. **Редизайн загрузки аватара — кружок-превью вместо файлового поля и отдельной кнопки**
     — `settings_account.html` сейчас показывает загрузку аватара как обычное файловое поле
     (`<input type="file" id="account-avatar">`) с подписью «Аватар» и отдельной кнопкой
     «Загрузить», требующей отдельного клика после выбора файла — то есть два шага там, где
     по референсу (GitHub-style, круглый аватар с иконкой-карандашом поверх при наведении)
     должен быть один. Убрать кнопку «Загрузить»: сама форма
     (`action="/settings/account/avatar"`) отправляется автоматически по событию `change`
     на `<input type="file">`, как только пользователь выбрал файл — эндпоинт на сервере не
     меняется, меняется только то, что его дёргает. Визуально — круглое превью текущего
     аватара (или инициалов, если аватара ещё нет, PR 88/90), кликабельное само по себе
     (открывает системный диалог выбора файла) и с полупрозрачной иконкой-карандашом при
     наведении/фокусе, подсказывающей, что элемент кликабельный. Коммиты: скрыть нативный
     `<input type="file">` и кнопку «Загрузить», сделать превью-кружок с оверлеем-карандашом
     кликабельным триггером выбора файла → автосабмит формы по `change` на `<input>` (JS) →
     превью выбранного файла до отправки (`FileReader`/`URL.createObjectURL`) → проверка
     состояния ошибки загрузки (`avatar_error`) без потери превью текущего аватара.

110. **Заголовок «Изменение пароля» на странице «Безопасность»** — `settings_security.html`
     сейчас выводит три поля пароля (`current_password`/`new_password`/`new_password_confirm`)
     без какого-либо заголовка секции над ними — на разделе с одной формой это не критично
     сейчас, но не соответствует референсу (скриншот) и не масштабируется, если в
     «Безопасность» позже добавится что-то ещё (например управление сессиями). Добавить
     `<h2>`/аналогичный заголовок секции «Изменение пароля» над формой, тем же паттерном
     заголовков, что уже используется в других местах настроек. Коммит: заголовок секции
     над формой смены пароля в `settings_security.html`, проверка вёрстки на десктопе и
     мобильных настройках (PR 104).

111. **Баг: кнопка «Фильтры» в каталоге не прижата к правому краю экрана** — диагностика:
     `.search-form__filters-toggle` (`app.css`, PR 98) уже имеет `margin-left: auto`, но
     этот `margin-left: auto` работает относительно `.search-form`, а не всего `.header` —
     сама `.search-form` задаёт `flex: 1; max-width: 480px;`, то есть на широких экранах
     останавливается в росте на 480px и не дотягивается до правого края `.header`
     (`display: flex`, `padding: 20px 32px`), в котором сейчас нет других flex-элементов,
     кроме самой `.search-form`. В результате кнопка «Фильтры» прижимается к правому краю
     480-пиксельной формы, которая сама находится где-то в левой части широкого экрана — а
     не к правому краю экрана/шапки, как ожидается. Исправить, вынеся кнопку «Фильтры» из
     `.search-form` в `.header` отдельным flex-элементом (соседом `.search-form`, не её
     потомком), с `margin-left: auto` уже на этом уровне — тогда её позиция считается
     относительно всей ширины `.header`, а не капа в 480px. Форма отправки (`form="catalog-
     search-form"` уже используется у чекбоксов фильтров тем же паттерном) остаётся прежней.
     Коммиты: подтверждение причины (диагностика выше) → перенос кнопки «Фильтры» из
     `.search-form` в `.header` как отдельного flex-элемента с собственным `margin-left:
     auto` → проверка на широких (2K+) и узких экранах → проверка мобильной раскладки
     (PR 103), чтобы кнопка не потерялась при переносе.

112. **Плавная перестройка сетки карточек при открытии/закрытии панели «Фильтры»** —
     `.catalog-layout` (`display: flex`) и `.catalog-layout__results` (`flex: 1; min-width:
     0;`) сейчас не задают `transition` на изменение ширины при появлении/скрытии
     `.catalog-filters` (PR 98) — сетка карточек (`.title-card-grid`, `grid-template-columns:
     repeat(auto-fill, minmax(140px, 1fr))`) при открытии панели фильтров резко
     перестраивается на новую доступную ширину в один кадр, а не плавно, в отличие от самой
     панели, которая уже анимирует появление (`catalog-filters-in`, PR 98). Добавить
     `transition` на ширину `.catalog-layout__results` (или на `flex-basis`/`width`
     `.catalog-filters`, если открытие/закрытие меняет её с 0), синхронизированный по
     длительности с существующей анимацией панели, чтобы сетка карточек плавно сжималась/
     расширялась вместе с панелью, а не скачком. Учесть находку PR 60 (лаги grid-layout при
     анимации ширины на каталоге с сотнями подгруженных карточек) — тот же риск
     применим и здесь, проверить производительность на длинном подгруженном списке.
     Коммиты: transition на ширину результатов/панели, синхронизированный с анимацией
     панели → проверка производительности на каталоге с сотнями карточек (риск из PR 60) →
     проверка на десктопных и мобильных брейкпоинтах (PR 103, где панель — полноэкранная
     модалка, а не боковая колонка).

113. **Баг: страница скроллится дальше после того, как долистан блок «Фильтры»** —
     `.catalog-filters` (`app.css`, PR 98) — `position: sticky; max-height: calc(100vh -
     32px); overflow-y: auto;` — свой скролл внутри панели, но без `overscroll-behavior`
     скролл, дойдя до конца списка опций внутри панели, «перетекает» на скролл всей
     страницы (`.catalog-layout__results`) под колёсиком мыши/тачпадом — стандартное
     поведение браузера для вложенных скролл-контейнеров, сбивающее с толку, когда
     пользователь долистал фильтры и продолжает скроллить, ожидая, что ничего не
     произойдёт. Добавить `overscroll-behavior: contain` на `.catalog-filters` (и на
     `.catalog-filters__options`, если скролл там, а не на самой панели), чтобы скролл
     останавливался на границе панели, не передаваясь странице. Коммит: `overscroll-
     behavior: contain` на прокручиваемый контейнер панели фильтров, проверка на десктопе
     (колесо мыши) и мобильных (свайп) — в т.ч. в полноэкранной модалке PR 103.

114. **Закрепить шапку и переключатели секций панели «Фильтры» при скролле списка внутри
     неё** — при появлении собственного скролла в `.catalog-filters__options` (PR 99, когда
     список опций раздела длиннее доступной высоты, например полсотни жанров) сейчас
     скроллится не только список опций, а вся `.catalog-filters` целиком как один
     `overflow-y: auto`-контейнер (см. диагностику PR 113 выше) — заголовок панели
     (`.catalog-filters__header` — название «Фильтры» + крестик закрытия) и заголовки-
     аккордеоны разделов (`.catalog-filters__section-toggle`, PR 99) уезжают из вида вместе
     с остальным содержимым, хотя должны оставаться на месте как элементы управления. Дать
     скролл собственно `.catalog-filters__options` каждого раздела (а не всей панели), с
     `.catalog-filters__header` и `.catalog-filters__section-toggle` как `position: sticky`
     внутри этого же скролл-контейнера (тот же принцип «несколько прилипающих подзаголовков
     в одном скролле», что уже применяется на других длинных списках сайта), чтобы кнопка
     закрытия и переключатели разделов оставались доступны, пока прокручивается содержимое
     конкретного раздела. Коммиты: перенос скролла с `.catalog-filters` на
     `.catalog-filters__options` внутри каждого раздела → `position: sticky` для
     `.catalog-filters__header` и `.catalog-filters__section-toggle` в новой раскладке →
     проверка при нескольких одновременно развёрнутых длинных разделах (жанры + страны).

115. **Баг: текст в выпадающем списке сортировки каталога не помещается в одну строку** —
     `.toc__export-format`/кастомный dropdown поверх него (`custom-dropdown.js`, PR 54)
     переиспользует общую высоту/паддинги `.toc__export-format` (`height: 36px; padding: 0
     10px;`), рассчитанные на короткие подписи форматов экспорта (txt/fb2/epub/pdf) —
     варианты сортировки каталога («По количеству глав», «По последней главе» и т.п.)
     заметно длиннее и переносятся на второй визуальный ряд внутри пункта списка вместо
     того, чтобы помещаться в одну строку, как на референсе. Расширить сам dropdown
     (ширину триггера и/или выпадающей панели) под самый длинный вариант сортировки, а не
     под общий с форматами экспорта размер, и/или запретить перенос строк в пунктах
     (`white-space: nowrap`) с адаптивной шириной по содержимому. Коммиты: диагностика
     (замер самого длинного варианта сортировки) → расширение ширины
     триггера/панели дропдауна сортировки (селектор, не задевающий dropdown формата
     экспорта) → `white-space: nowrap` на пунктах списка → проверка на мобильных
     брейкпоинтах, где ширина шапки каталога и так ограничена (PR 19/103).

116. **Убрать скроллбар сайдбара** — `.sidebar` (`app.css`) задаёт `height: 100vh;
     overflow-y: auto;` с колонкой из иконок-пунктов (`gap: 22px`) и аватаркой профиля
     внизу (PR 88/93) — на части экранов (невысокие окна/ноутбучные экраны) суммарная
     высота содержимого чуть превышает `100vh`, и браузер показывает вертикальный
     скроллбар прямо в узкой 64-пиксельной колонке — визуально ломает дизайн icon-only
     сайдбара, для которого скролл не задумывался как обычный сценарий использования.
     Не убирать `overflow-y: auto` совсем (на случай реального переполнения при большом
     количестве пунктов меню в будущем), а скрыть сам визуальный скроллбар, сохранив
     функциональность скролла — `scrollbar-width: none` (Firefox) и
     `.sidebar::-webkit-scrollbar { display: none; }` (Chromium/Safari), тот же принцип,
     что и кастомизация скроллбара в PR 55, но для сайдбара — полное скрытие, а не
     перекраска, раз в 64px нет места для видимого скроллбара без ужатия иконок. Коммиты:
     скрытие скроллбара сайдбара (`scrollbar-width`/`::-webkit-scrollbar`) → проверка, что
     скролл всё ещё работает при реальном переполнении (временно добавить лишние пункты
     для теста) → проверка в развёрнутом состоянии сайдбара (PR 39).

117. **Быстрый просмотр тайтла — иконка глаза на карточке и модалка** — карточки тайтлов
     (`_title_card.html`, макрос `title_card`) сейчас — это целиком один `<a>`-элемент,
     ведущий на страницу тайтла; добавить в левом верхнем углу карточки кнопку с иконкой
     глаза, открывающую по клику модальное окно с быстрым просмотром тайтла (без перехода
     на отдельную страницу) — обложка, название, краткое описание/метаданные, что уже
     доступно на странице тайтла. Поскольку вся карточка — ссылка, кнопка внутри нужно
     явно останавливать всплытие и отменять переход (`event.preventDefault()`/
     `stopPropagation()` в обработчике клика по кнопке), иначе клик по глазу будет и
     открывать модалку, и уводить на страницу тайтла одновременно. Модалка закрывается
     кликом вне её области или крестиком — тот же паттерн, что уже есть у других модалок/
     панелей сайта (полноэкранный просмотр изображений PR 66, выпадающее меню профиля
     PR 93 — клик вне закрывает). Данные для модалки — переиспользовать уже существующий
     эндпоинт/шаблон страницы тайтла (частичный рендер/фрагмент), не дублировать логику
     сборки метаданных тайтла в отдельном месте. Коммиты: иконка глаза на карточке
     (`_title_card.html`, доступна с клавиатуры — `button`, не `div`) → обработчик клика с
     остановкой всплытия/перехода → модалка быстрого просмотра (открытие/закрытие по
     крестику и клику вне области) → эндпоинт/фрагмент с данными тайтла для модалки,
     переиспользующий существующую сборку метаданных → проверка на карточках «Недавние»,
     «Читаю» и в каталоге.

118. **Увеличить масштаб интерфейса внутри панели «Фильтры»** — `.catalog-filters__section-
     toggle` (`font-size: 12px`) и `.catalog-filters__option` (`font-size: 13px`) заметно
     мельче остального текста интерфейса (референс — обычные интернет-магазины с панелью
     фильтров, где шрифт фильтров не мельче остального контента) — сейчас панель выглядит
     убористой и плохо читаемой по сравнению с остальным сайтом. Увеличить размер шрифта
     заголовков разделов и пунктов-опций до одного уровня с остальным текстом настроек/
     форм сайта, пропорционально увеличив отступы и размер кастомных чекбоксов (PR 53),
     чтобы читаемость выросла, а не только размер текста при том же тесном интервале.
     Коммиты: увеличение `font-size` `.catalog-filters__section-toggle` и
     `.catalog-filters__option` до общего масштаба сайта → пропорциональная правка
     отступов/`gap` внутри пунктов → пропорциональное увеличение кастомных чекбоксов
     (PR 53) под новый размер текста → проверка, что панель по-прежнему помещается по
     высоте (`max-height: calc(100vh - 32px)`, PR 98) с более крупным содержимым.

119. **Баг: кнопка «Открыть страницу тайтла» в модалке быстрого просмотра растянута на всю
     ширину и подчёркнута** — диагностика: `_title_quickview.html` (PR 117) рендерит её как
     `<a class="btn btn--primary" href="...">`, единственный/последний ребёнок
     `.title-hero__info` — а `.title-hero__info { display: flex; flex-direction: column;
     gap: 8px; }` не задаёт `align-items`, поэтому по умолчанию действует `stretch`: каждый
     flex-элемент колонки, включая саму ссылку-кнопку, растягивается на всю ширину
     контейнера — отсюда широкая плашка на весь бок модалки вместо компактной кнопки.
     Подчёркивание — отдельная причина: `.btn` нигде не задаёт `text-decoration: none`,
     потому что до этого места `.btn` всегда применялся к `<button>`, где подчёркивать
     нечего — это первое место в проекте, где `.btn` используется на `<a>`. Коммиты:
     `align-items: flex-start` на `.title-hero__info` (или `align-self: flex-start` только
     на самой кнопке, если растягивать остальные элементы колонки по-прежнему нужно) →
     `text-decoration: none` на `.btn` → проверка остальных мест, где `.btn` уже
     применяется к `<a>` (например `_locked_feature.html`), на случай того же скрытого
     подчёркивания.

120. **Баг: сайдбар прокручивается у авторизованных пользователей** — `.sidebar` (`height:
     100vh; overflow-y: auto;`, PR 116 скрыл сам скроллбар, но не убрал сам скролл) у
     авторизованного пользователя дополнительно несёт `.sidebar__account` (аватар/меню
     профиля, PR 88/93) внизу колонки через `margin-top: auto` — суммарная высота пунктов
     меню (`gap: 22px` между иконками, PR 23/39) плюс блок аккаунта на невысоких
     окнах/ноутбучных экранах превышает `100vh`, и `overflow-y: auto` реально включает
     скролл (просто с невидимым скроллбаром после PR 116, что не то же самое, что «скролла
     нет»). В icon-only колонке шириной 64px сайдбар по своей природе не должен требовать
     скролла для базового набора пунктов меню. Коммиты: замер реальной высоты содержимого
     сайдбара с `.sidebar__account` на характерных невысоких разрешениях (например
     1366×768) → уменьшение `gap`/отступов между пунктами меню и/или высоты блока аккаунта
     ровно настолько, чтобы контент гарантированно помещался в `100vh` в обычных случаях →
     `overflow-y: auto` остаётся как подстраховка на случай будущего роста числа пунктов
     меню, а не убирается совсем → проверка на нескольких характерных разрешениях по
     высоте, включая развёрнутое состояние сайдбара (PR 39).

121. **Бесшовный переход между разделами настроек (доработка PR 89/108)** — PR 108 решил
     мигание сайдбара при переключении состояния «развёрнут/свёрнут» между полными
     перезагрузками страниц, но само мигание при переходе между вкладками `/settings/*`
     (Аккаунт/Безопасность/Чтение) — отдельная причина: PR 46 завёл `mobile-section-in`
     (плавное появление `.main`) только внутри `@media (max-width: 640px)` — на десктопе
     между полными перезагрузками страниц (это server-rendered MPA без клиентского роутера)
     ничего не сглаживает переход, отсюда обычная для многостраничных сайтов резкая
     перерисовка, которая здесь особенно заметна на сайдбаре из-за смены подсветки активного
     пункта. Использовать нативный CSS View Transitions API для навигации между документами
     (`@view-transition { navigation: auto; }`) — сначала точечно для `/settings/*`
     (однотипный layout между под-роутами, наименьший риск визуальных артефактов), с плавной
     деградацией без анимации в браузерах без поддержки (сейчас это Chromium-браузеры) —
     то есть не хуже текущего поведения там, где API нет. Коммиты: `@view-transition` для
     навигации между `/settings/*` → проверка отсутствия резких скачков сайдбара/контента
     при переключении Аккаунт/Безопасность/Чтение в поддерживающих браузерах → проверка
     graceful fallback в браузерах без поддержки API (не хуже текущего состояния) → (опция)
     расширение на остальную навигацию сайта отдельным пунктом, если результат на
     `/settings/*` устроит.

122. **Публичная страница профиля с разделами (пересматривает PR 92)** — PR 92 сделал
     `/profile` только с аватаром/никнеймом-или-email/bio/датой регистрации. Расширить её
     разделами: (1) что пользователь читает сейчас — название тайтла ссылкой (без карточки,
     просто текстовая ссылка на `/titles/{slug}`), под ним том и номер/название главы; берётся
     из `library_entries` (`app/db/library.py`) — запись с самым свежим `last_read_at`
     (`list_entries()` уже возвращает их в этом порядке, `list_entries(...)[0]`, если у него
     есть `last_read_at`); (2) сама библиотека тайтлов пользователя (переиспользует
     карточки, PR 16, и вкладку «Читаю», PR 14) под секцией «читает сейчас». Оба новых
     раздела рендерятся пока безусловно (без настроек приватности — это отдельный пункт,
     PR 124, который добавляет возможность их скрыть) и относятся к владельцу профиля;
     чужой профиль показывает то же самое для чужого `user_id`, без ограничения «только я
     сам» — сама модерация видимости через приватность приходит отдельным PR. Коммиты:
     секция «Читает сейчас» (запрос самой свежей `library_entries`-записи, ссылка на
     тайтл, том/глава) → секция «Библиотека» на странице профиля (переиспользование карточек
     PR 16) → проверка для пользователя без записей в библиотеке (секции не показываются,
     а не показываются пустыми) → проверка на чужом профиле (не только на себе).

123. **Избранный тайтл в библиотеке и на странице профиля** — новая возможность отметить
     один тайтл в личной библиотеке как избранный: колонка `is_favorite` в
     `library_entries` (миграция `0008_library_entries_favorite.sql`, `app/db/migrations/`
     — следующая по номеру после уже существующей `0007_users_avatar_path.sql`), кнопка
     «Добавить/убрать из избранного» в интерфейсе библиотеки (`library.html`/карточка
     тайтла), и рендер этого тайтла на странице профиля (PR 122) с увеличенной/акцентной
     рамкой карточки, отдельно от обычной сетки библиотеки. Ровно один избранный тайтл на
     пользователя проще всего реализовать как обычный булев флаг с очисткой предыдущего
     избранного при выборе нового (`UPDATE library_entries SET is_favorite = 0 WHERE
     user_id = ?` перед установкой нового), не вводя отдельную таблицу ради одного
     значения. Коммиты: миграция `is_favorite` → функции добавить/снять избранное в
     `app/db/library.py` (со сбросом предыдущего) → кнопка «В избранное» в UI библиотеки/
     карточки → секция «Избранное» на странице профиля с акцентной рамкой карточки →
     проверка, что повторный выбор нового избранного корректно снимает старое.

124. **Настройки приватности профиля (раздел «Аккаунт» → «Приватность»)** — три независимых
     переключателя, определяющих, что видно на публичной странице профиля (PR 122)
     посторонним посетителям (сам владелец видит свой профиль как есть, без учёта этих
     флагов, у него же должна быть возможность отредактировать то, что он выключил из
     публичного показа): показывать ли «что читает сейчас», показывать ли избранный тайтл
     (PR 123 — переключатель без смысла, пока самой фичи ещё нет, поэтому идёт после неё),
     показывать ли библиотеку целиком. Три новые колонки в `users` (миграция
     `0009_users_privacy_flags.sql`, следующая по номеру после `0008` из PR 123) со
     значением по умолчанию «показывать» (не менять поведение PR 122 для тех, кто ни разу
     не заходил в настройки приватности). Коммиты: миграция трёх флагов приватности →
     форма переключателей в разделе «Аккаунт» → применение флагов при рендере `/profile`
     чужого профиля (у владельца ничего не скрывается) → проверка всех восьми комбинаций
     трёх независимых переключателей.

125. **Тест скорости чтения — отдельная модалка с закреплённым таймером** — сейчас тест
     (`reading-speed-test.js`, PR 77) разворачивается прямо в потоке страницы настроек:
     образец текста и статус появляются под кнопкой «Начать тест» без какого-либо
     обособления. Вынести это в модальное окно (тот же паттерн, что и `.title-quickview-
     modal`, PR 117, — фон-подложка, клик вне области закрывает, крестик закрытия) с
     таймером, закреплённым во время теста, но ненавязчивым (например небольшая плашка в
     углу модалки, а не крупная надпись поверх текста). Крестик закрытия прерывает тест
     (тот же эффект, что истечение времени без клика — тест просто не засчитывается, ничего
     не сохраняется в `readingSpeedWpm`). Коммиты: модалка вокруг существующей разметки
     теста (образец текста, статус, кнопка «Начать») → закреплённый ненавязчивый таймер
     внутри модалки → крестик закрытия и закрытие по клику вне области, прерывающие
     незавершённый тест → проверка, что ручной ввод скорости (PR 78, вне модалки) не
     затронут.

126. **Убрать hover-эффект с аватара пользователя** — `.sidebar__avatar:hover { background:
     var(--accent-teal); color: var(--bg-page); }` (PR 88, внутри `@media (hover: hover)`)
     сейчас перекрашивает круглый аватар/инициалы в акцентный цвет при наведении — при
     открытии по клику меню профиля (PR 93/97) сама интерактивность аватара уже достаточно
     очевидна (курсор-поинтер, открывающееся меню), а перекраска инициалов/фото при
     наведении читается скорее как лишний, отвлекающий эффект, чем как полезная подсказка.
     Убрать это конкретное hover-правило, не трогая курсор/доступность самой кнопки.
     Коммит: удалить `.sidebar__avatar:hover` (или заменить на нейтральный `cursor: pointer`
     без смены цвета, если хочется всё же обозначить кликабельность), проверить, что
     остальная кликабельность аватара (открытие меню, `:focus-visible`) не пострадала.

127. **Кнопка «наверх» смещается при открытой панели «Фильтры» в каталоге** — `.back-to-top`
     (`position: fixed; bottom: 24px; right: 32px;`, PR 102) и `.catalog-filters`
     (`position: sticky`, PR 98, флекс-элемент шириной `240px` рядом с сеткой карточек)
     сейчас никак не координируют свою позицию — на десктопе панель фильтров, когда
     открыта, занимает правую колонку и может визуально перекрываться с кнопкой «наверх»,
     закреплённой у того же правого края экрана. Сдвигать `.back-to-top` левее (например
     добавлять к `right` ширину открытой панели плюс отступ) на время, пока
     `.catalog-filters` открыта, синхронно с её собственной анимацией появления/скрытия
     (PR 98). Коммиты: CSS-переменная или модификатор-класс для смещённой позиции
     `.back-to-top` при открытых фильтрах → JS-переключение этого состояния синхронно с
     `catalog-filters-toggle.js` → проверка на десктопе (боковая колонка) и на мобильных,
     где `.catalog-filters` — полноэкранная модалка (PR 103, там кнопку «наверх», скорее
     всего, логичнее на время просто прятать, а не сдвигать).

128. **Placeholder'ы в текстовых полях аутентификации** — `login.html`/`register.html`
     сейчас не задают `placeholder` ни для email, ни для пароля (в отличие, например, от
     `.search-input`, у которого он уже есть) — пустые поля без подсказки формата выглядят
     менее завершённо, чем остальной сайт. Добавить `placeholder` (например
     `you@example.com` для email, «Минимум 8 символов»/аналогичная подсказка требований для
     пароля, если у пароля есть минимальная длина на бэкенде — как есть на самом деле, а не
     произвольное число) на оба поля в обеих формах. Коммиты: placeholder для полей email
     (login/register) → placeholder для полей пароля (login/register/повтор пароля) →
     проверка, что placeholder не подставляется как значение при пустой отправке формы.

129. **Автоскролл к сохранённому прогрессу при открытии главы (оба режима чтения)** — сейчас
     ни `tap-to-read.js` (PR 62/76), ни `reader-progress.js` (PR 84) не восстанавливают
     позицию скролла на загрузке страницы — `tap-to-read.js` намеренно вызывает
     `reveal(loadRevealedCount())` без `{ scroll: true }` (см. комментарий в самом файле:
     скролл сразу после восстановления множества абзацев может конфликтовать с тем, куда
     сам браузer/ридер уже поставил начальный скролл), а `reader-progress.js` вообще только
     пишет прогресс через `IntersectionObserver`, ничего не восстанавливая на старте. Оба
     режима уже пишут в один и тот же ключ `tapToReadProgress:{путь}` (PR 83/84) в формате
     `{ revealed, total }`, так что источник для автоскролла один и тот же независимо от
     режима. После того как контент главы отрисован (и, для тапа, после восстановления
     `revealed` абзацев), одним отдельным шагом прокручивать страницу к абзацу с индексом
     `revealed - 1` (`scrollIntoView` с разумным отступом сверху, а не `block: "end"`, чтобы
     не упираться в самый низ экрана сразу на старте) — так, чтобы пользователь, открыв уже
     начатую главу, сразу видел место, на котором остановился, а не начало главы. Коммиты:
     чтение сохранённого `revealed` из `tapToReadProgress:` при загрузке страницы главы
     (общий шаг для обоих режимов) → автоскролл к этому абзацу после отрисовки в обычном
     режиме → тот же автоскролл в режиме тапа, согласованный с уже существующим
     восстановлением `revealed` абзацев без анимации разворачивания (PR 76) → проверка на
     главе без сохранённого прогресса (скролл остаётся у начала, ничего не ломается) и на
     главе, прочитанной полностью (скролл к последнему абзацу, а не к невидимому «следующему»).

130. **Баг: автоскролл при раскрытии абзаца в режиме тапа не докручивает до конца страницы
     (доработка PR 76)** — PR 76 добавил `scrollIntoView({ behavior: "smooth", block: "end"
     })` на только что раскрытый абзац в `reveal()`/`revealNextWithTempo()`
     (`tap-to-read.js`) — технически автоскролл работает (в отличие от полного отсутствия
     скролла, которое чинил сам PR 76), но `block: "end"` подводит нижний край раскрытого
     абзаца ровно к нижнему краю экрана, из-за чего сразу под текстом становится видна
     подсказка «Тапните, чтобы читать дальше» и панель навигации «Предыдущая/Следующая
     глава» — ощущается как «слишком рано», а не как комфортный отступ. Нужно докручивать
     страницу до самого её низа (`document.body`/`document.documentElement.scrollHeight`),
     а не только до нижнего края конкретного абзаца, чтобы после раскрытия на экране
     оставался только новый текст с нормальным воздухом снизу, без немедленно видимой
     навигации. Коммиты: замена `lastRevealed.scrollIntoView({ block: "end" })` на скролл
     всего документа к его текущему нижнему краю в `reveal()` → то же самое в
     `revealNextWithTempo()` (тёмповые анимации PR 79) → проверка на короткой главе, где
     раскрытый текст не заполняет весь экран (скролл не должен уводить страницу дальше, чем
     реально есть контента) → проверка совместно с PR 129 (автоскролл к сохранённому
     прогрессу при первой загрузке не должен путаться с этим автоскроллом на каждый тап).

131. **Кастомное контекстное меню на абзаце** — инфраструктура для PR 132/133: правый клик
     (`contextmenu`, с `event.preventDefault()`, чтобы подавить нативное меню браузера) на
     блоке-обёртке абзаца в режиме тапа (`.reader-content__paragraph`, обёртки
     `tap-to-read.js`) или на самом абзаце в обычном режиме (`[...content.children]`,
     `reader-progress.js`) открывает компактное меню, заякоренное на месте клика. Слушатель
     вешается делегированно на `.reader-content` (`data-role="chapter"`), а не на каждый
     абзац по отдельности — контент приходит из `chapter.content` SDK одним HTML-блоком, и
     количество/структура его дочерних элементов заранее неизвестны. Ключ конкретного
     абзаца для меню (и для PR 132/133 ниже) — `slug_url` + `volume` + `number` + `branch_id`
     (те же поля, что уже пишет `record_progress()`/`record_chapter_read()`,
     `app/db/library.py`/`app/db/activity.py`) плюс порядковый индекс абзаца среди детей
     `.reader-content` — тот же индекс, что уже использует и `tap-to-read.js`, и
     `reader-progress.js` для отслеживания прогресса, так что его надёжность как анкера уже
     проверена практикой, ничего нового не изобретается. Меню — портал в `<body>` с
     `position: fixed` и координатами по месту клика (тот же приём, что уже решал проблему
     обрезки `.profile-menu__panel` overflow-родителем, PR 97), закрывается кликом вне
     области или Escape. Пункты меню на этом шаге — заглушки (сами реакции/комментирование
     подключаются в PR 132/133). Анонимному посетителю меню всё равно открывается, но
     пункты действий ведут на `/login` вместо выполнения действия (тот же принцип, что
     `_locked_feature.html` для остальных персональных фич). Коммиты: делегированный
     обработчик `contextmenu` на `.reader-content` в обоих режимах чтения → вычисление ключа
     абзаца (slug/volume/number/branch_id/индекс) → портал-меню с позиционированием по
     клику, закрытие по клику вне области/Escape → заглушки пунктов меню → поведение для
     анонимного посетителя (ссылка на `/login`).

132. **10 базовых эмодзи-реакций на абзац** — пункт в меню PR 131, открывающий полосу из
     10 эмодзи (👍 ❤️ 😂 😮 😢 😡 🔥 👏 🤔 💯 — конкретный набор можно скорректировать при
     реализации, важно ровно десять, как попросил пользователь); клик по эмодзи сохраняет
     реакцию текущего пользователя на этот абзац, повторный клик по той же эмодзи убирает
     её — один пользователь может оставить не более одной активной реакции на конкретный
     абзац одновременно (переключение, а не накопление). Новая таблица `reactions`
     (миграция `0010_reactions.sql`, следующая по номеру после `0009_users_privacy_flags.sql`)
     — те же денормализованные поля, что и в PR 131 (`slug_url`/`volume`/`number`/
     `branch_id`/`paragraph_index`), без внешнего ключа на несуществующую локальную таблицу
     глав — SDK остаётся источником истины по самому контенту, здесь хранится только сам
     факт реакции (тот же принцип, что уже описан в docstring `app/db/library.py`: не
     дублировать данные SDK). Агрегированные счётчики по каждой эмодзи отображаются под
     абзацем: в режиме тапа — небольшой ряд иконок-реакций под чат-плашкой (перекликается со
     стилем переписки PR 64 — как реакции на сообщение в мессенджере), в обычном режиме —
     компактная полоса при наведении/фокусе на абзац. Коммиты: миграция `reactions` →
     эндпоинт поставить/снять реакцию → пункт «Реакции» в контекстном меню PR 131 с полосой
     из 10 эмодзи → агрегированные счётчики под абзацем (оба режима чтения) → проверка
     переключения реакции (клик по уже стоящей эмодзи снимает её).

133. **Комментирование абзаца** — пункт «Комментировать» в меню PR 131 открывает форму
     (текстовое поле + «Отправить»), создающую комментарий, привязанный к тому же ключу
     абзаца, что и PR 131/132. Новая таблица `comments` (миграция
     `0011_comments.sql`, следующая по номеру после `0010_reactions.sql` из PR 132) —
     `slug_url`/`volume`/`number`/`branch_id`/`paragraph_index` тем же паттерном, плюс
     `parent_comment_id` (nullable, self-reference) для ответов на комментарии с самого
     начала, а не отдельным пунктом позже — reddit-подобная вложенность была частью
     исходного запроса, а не более поздней доработкой. Под абзацем — счётчик существующих
     комментариев со стрелкой разворачивания (например «100 комментариев ▾»), сам список
     комментариев подгружается лениво только по клику на стрелку, а не рендерится сразу для
     каждого абзаца главы — иначе страница длинной главы с активным обсуждением тянула бы
     сотни комментариев на каждый скролл. Внутри развёрнутого списка — reddit-like вёрстка:
     вложенные ответы с отступом по глубине, у каждого комментария своя кнопка «Ответить»,
     открывающая ту же форму создания с проставленным `parent_comment_id`. Анонимный
     посетитель видит уже оставленные комментарии, но форма создания/кнопка «Ответить» ведут
     на `/login` (тот же принцип, что PR 131/`_locked_feature.html`). Коммиты: миграция
     `comments` (с `parent_comment_id`) → эндпоинт создания комментария (в т.ч. ответа) →
     эндпоинт списка комментариев абзаца, отдающего дерево, а не плоский список → счётчик +
     стрелка разворачивания под абзацем (оба режима чтения) → ленивая подгрузка списка по
     клику → reddit-like вложенная вёрстка с отступами по глубине → кнопка «Ответить» на
     каждом комментарии → поведение для анонимного посетителя (чтение доступно, создание —
     через `/login`).

134. **Настройка чтения: скрыть реакции и комментарии** — не всем понравится вид абзаца с
     полосой реакций и счётчиком комментариев под ним (PR 132/133), особенно тем, кто просто
     читает. Новый переключатель в настройках чтения (`/settings/reading`, тот же
     `readerSettings`-паттерн, что и остальные настройки типографики/тапа, PR 30/63) —
     например `showParagraphSocial`, по умолчанию включён (не менять поведение PR 132/133
     для тех, кто ни разу не заходил в настройки). При выключении скрывается не только
     визуальная полоса реакций/счётчик комментариев под абзацами (оба режима чтения), но и
     сам пункт вызова контекстного меню PR 131 по правому клику на абзаце — если реакции и
     комментарии выключены, открывать меню, ведущее ровно к ним, нечего. Коммиты:
     переключатель в разделе настроек чтения → скрытие полосы реакций/счётчика комментариев
     под абзацами при выключенной настройке (оба режима) → отключение контекстного меню
     PR 131 при выключенной настройке → проверка, что уже существующие
     реакции/комментарии никуда не удаляются, а просто не показываются, пока настройка
     выключена.

135. **Профиль: счётчик сообщений пользователя** — на странице профиля (PR 122) показывать
     общее количество комментариев, оставленных пользователем (`comments`, PR 133) — простой
     `COUNT(*) WHERE user_id = ?`, рядом с остальными данными профиля (аватар/никнейм/bio).
     Коммиты: запрос количества комментариев пользователя (`app/db/comments.py`) → вывод
     счётчика на странице профиля → проверка для пользователя без единого комментария (0,
     а не пустая строка/ошибка).

136. **Профиль: календарь чтения (GitHub-style heatmap)** — на странице профиля (PR 122)
     календарь активности по дням за последний год, визуально по мотивам GitHub-графика
     коммитов, но стилизованный под тёмную тему и акцентный цвет сайта (`--accent-teal`)
     вместо GitHub-овской зелёной шкалы. Источник данных — уже существующая таблица
     `activity_events` (миграция `0004_activity_events.sql`, PR 18): группировка событий
     `kind = 'chapter_read'` (и/или `heartbeat`, по количеству активных секунд) по
     календарному дню (`created_at`, UTC — та же временна́я зона, что уже использует
     `app/db/activity.py` для «сегодня» в разделе «Активность»), с интенсивностью цвета
     клетки по объёму чтения в этот день, ничего заново не подсчитывая на SDK-стороне.
     Коммиты: запрос агрегированной активности по дням за 52 недели (`app/db/activity.py`) →
     компонент сетки-календаря (вёрстка/CSS в акцентной палитре сайта) → шкала интенсивности
     цвета клетки по объёму активности за день → tooltip с точной датой и количеством при
     наведении на клетку → проверка для пользователя без истории чтения (пустой календарь,
     не ошибка).

137. **Баг: подменю реакций/комментариев открывается и сразу закрывается** — диагностика:
     `paragraph-menu.js` (PR 131) держит один `document`-level `click`-слушатель, который
     закрывает панель, если клик пришёлся не по ней (`!panel.contains(event.target)`). Клик
     по пункту «Реакции»/«Комментировать» сначала выполняет свой собственный обработчик
     (`renderReactionPicker`/`renderCommentComposer`), а тот начинается с
     `panel.replaceChildren()` — это удаляет из DOM саму кнопку, по которой только что
     кликнули (`reactItem`/`commentItem`), вместе с остальным старым содержимым панели, и
     заменяет его инвентарём/списком эмодзи. Событие клика продолжает всплытие по пути,
     вычисленному ДО этой замены (так работает DOM), и в итоге доходит до `document`-level
     слушателя — но к этому моменту `event.target` уже отсоединён от документа и, что
     важнее, от `panel` (был в `panel`, но `replaceChildren()` вынул его оттуда), поэтому
     `panel.contains(event.target)` возвращает `false`, хотя клик по факту пришёлся строго
     внутри панели — `close()` срабатывает мгновенно следом за открытием, отсюда «на долю
     секунды и сразу пропадают». Тот же риск есть и у кнопки «← Назад» (тоже вызывает
     `renderMenuItems`/`replaceChildren()` изнутри собственного клика). Починить —
     `event.stopPropagation()` в обработчиках `reactItem`/`commentItem`/`back`, чтобы клик
     по ним не доходил до `document`-level слушателя вообще, а не полагаться на то, что цель
     клика останется в DOM к моменту, когда слушатель до неё доберётся. Коммиты:
     подтверждение причины (диагностика выше, воспроизвести на «Реакциях» и
     «Комментировать») → `event.stopPropagation()` в обработчиках всех кнопок, которые сами
     вызывают `panel.replaceChildren()` → проверка, что клик вне панели по-прежнему её
     закрывает (обычный сценарий не сломан) → проверка на кнопке «← Назад».

138. **Редизайн страниц настроек под уровень готового продукта** — `/settings/*` (Аккаунт/
     Безопасность/Чтение/Приватность) сейчас — это функционально верный, но визуально
     плоский набор лейблов/инпутов/чекбоксов без выраженной иерархии (см. скриншот) —
     читается как черновик форм, а не как экран продукта, которым пользуются массово. Как и
     остальная вёрстка сайта (см. CLAUDE.md, «Дизайн»), это не повод изобретать разметку на
     месте — сначала re-импортировать актуальное состояние экранов настроек через
     `claude_design` MCP (тот же проект/страница, что уже указаны в CLAUDE.md), проверить,
     есть ли там более проработанная концепция именно для внутренних экранов настроек
     (Concept 3 могла не покрывать этот уровень детализации при первом импорте, PR 89), и
     перенести её в Jinja2-шаблоны тем же способом, что и остальной сайт — если её там нет,
     явно зафиксировать это и только тогда точечно дорабатывать текущую вёрстку (группировка
     полей в карточки, интервалы, типографическая иерархия лейблов/подписей/значений,
     единообразные состояния фокуса) вместо разрозненных правил. Коммиты: проверка
     `claude_design` MCP на актуальную концепцию экранов настроек (или явная фиксация, что
     её нет) → перенос/дизайн визуальной иерархии для `/settings/account` →
     `/settings/security` → `/settings/reading` (без изменения уже отлаженной логики
     типографики/тапа/скорости чтения внутри) → проверка на мобильных настройках (PR 104).

139. **Баг: календарь чтения рендерится высоким одним столбцом вместо широкой 7-строчной
     сетки** — `.reading-calendar { display: grid; grid-auto-flow: column;
     grid-template-rows: repeat(7, 1fr); }` (PR 136) на практике не переносит ячейки в
     новый столбец после седьмой, а тянет их одну под другой (см. скриншот — сотни клеток
     высоким столбцом вместо ожидаемой широкой полосы в ~53 колонки). Наиболее вероятная
     причина: `1fr` в `grid-template-rows` рассчитывается относительно блочного размера
     грид-контейнера по этой оси, а у `.reading-calendar` высота — `auto` (ничем не
     ограничена сверху и не задана явно), то есть определённого размера для распределения
     `fr`-долей нет — это ровно тот случай, из-за которого гитхаб-подобные тепловые карты на
     CSS Grid обычно строят строки фиксированным размером, а не `fr`. Заменить
     `grid-template-rows: repeat(7, 1fr)` на `grid-template-rows: repeat(7, 11px)` (тот же
     размер, что и так уже зашит явно в `.reading-calendar__day { width: 11px; height:
     11px; }`), чтобы высота строк не зависела от неопределённого размера контейнера.
     Коммиты: подтверждение причины (диагностика выше, проверить computed grid в
     девтулзах) → замена `1fr` на фиксированный `11px` в `grid-template-rows` → проверка
     формы на разных объёмах истории (пользователь с пустым календарём и с полным годом) →
     проверка `overflow-x: auto`-скролла на мобильных при уже исправленной широкой форме.

140. **Тултип клетки календаря должен показывать время активного чтения** — сейчас `title`
     каждой клетки (`_build_reading_calendar()`, `app/api/profile.py`) — это
     `f"{date}: {_pluralize_chapters(count)}"`, где `count` — число прочитанных глав в этот
     день (`daily_reading_activity()`, считает только `kind = 'chapter_read'`) — активное
     время чтения там не участвует вовсе, хотя оно уже отдельно считается для раздела
     «Активность» (`total_active_seconds_today()`, `app/db/activity.py`, сумма `seconds` по
     `kind = 'heartbeat'` за день). Добавить дневную сумму `heartbeat`-секунд в тот же
     тултип, тем же способом агрегации по дню, что `daily_reading_activity()` уже делает для
     `chapter_read` (`GROUP BY date(created_at)`), отформатировав в человекочитаемый вид
     (например «2 ч 15 мин» / «45 мин»), не меняя при этом сам расчёт интенсивности клетки
     (`level`, который может по-прежнему опираться на число глав — пользователь просил
     именно тултип, не перекраску шкалы). Коммиты: агрегатор дневных `heartbeat`-секунд в
     `app/db/activity.py` (аналог `daily_reading_activity()`, но `kind = 'heartbeat'`,
     `SUM(seconds)` вместо `COUNT(*)`) → форматирование секунд в «Ч ч M мин» →
     подстановка в `label`/`title` каждой клетки в `_build_reading_calendar()` → проверка
     на дне без активного времени (0 чтения, но есть прочитанные главы, и наоборот).

141. **Hover-эффект для карточек тайтлов: увеличение обложки и лёгкое затемнение** — сейчас
     `.title-card:hover` меняет цвет только у `.title-card__name` (см. `app.css`) —
     `.title-card__cover` (`<img>`, PR 16) никак не реагирует на наведение. Добавить лёгкий
     зум обложки (`transform: scale(...)`) и полупрозрачный тёмный градиент поверх неё при
     наведении. `<img>` не поддерживает генерируемое содержимое (`::before`/`::after`) ни в
     одном браузере, поэтому оверлей-градиент нельзя навесить прямо на сам `.title-card__cover`
     — нужен оборачивающий контейнер (`<span class="title-card__cover-wrap">` вокруг `<img>`
     в `_title_card.html`, `position: relative; overflow: hidden;`) с `::after`,
     позиционированным `inset: 0`, `background: linear-gradient(...)`, `opacity: 0`, и
     `.title-card:hover & { opacity: 1; }` — зум применяется к самому `<img>` внутри этого
     контейнера (`overflow: hidden` на обёртке не даёт увеличенному изображению вылезать за
     скруглённые углы карточки). Коммиты: обёртка `.title-card__cover-wrap` вокруг `<img>`
     в `_title_card.html` → CSS-затемняющий градиент через `::after` с переходом
     прозрачности → `transform: scale(...)` на `<img>` при наведении с `transition` →
     проверка, что скругление углов (`border-radius`) по-прежнему не нарушается при зуме.

142. **Просмотр обложки тайтла в полноэкранном режиме** — `.title-hero__cover`
     (`title.html`) сейчас обычный `<img>` без какого-либо взаимодействия по клику, в
     отличие от изображений внутри главы, у которых уже есть полноэкранный просмотр
     (`image-lightbox.js`, PR 66/74) с зумом/скачиванием. Расширить `image-lightbox.js` (а
     не заводить отдельный, дублирующий компонент) на дополнительный источник изображений —
     обложку тайтла на странице тайтла (при желании и в модалке быстрого просмотра, PR 117,
     где обложка тоже есть) — как одноэлементную «галерею» той же логики зума/скачивания,
     просто без стрелок навигации (`multiple` уже определяется по `images.length > 1`,
     ничего дополнительно проектировать не нужно). Коммиты: расширение селектора источников
     `image-lightbox.js` на `.title-hero__cover` → проверка, что стрелки навигации
     корректно скрыты для одиночной обложки → (опционально) то же для обложки в
     `.title-quickview-modal` (PR 117) → проверка, что клик по обложке не конфликтует с
     остальной кликабельностью `.title-hero`/модалки.

143. **Баг: скачивание изображения из полноэкранного просмотра уводит на ranobelib.me
     вместо загрузки файла** — диагностика: `image-lightbox.js`'s `.image-lightbox__download`
     — это `<a download href="...">`, но `download`-атрибут браузеры полноценно уважают
     только для одноресурсных (same-origin) ссылок — изображения главы приходят прямо из
     `chapter.content` SDK как хотлинки на исходный домен ranobelib.me (сайт их не
     перезаливает и не проксирует), то есть ссылка кросс-доменная, и браузер в этом случае
     молча игнорирует `download`, просто переходя по ссылке — открывается/загружается
     страница на ranobelib.me вместо скачивания файла. Полагаться на клиентский
     `fetch()` + blob-URL тоже не гарантированно надёжно — работает только если CDN
     ranobelib.me отдаёт нужные CORS-заголовки, а это вне нашего контроля и может
     измениться. Надёжнее — маленький проксирующий эндпоинт на своём бэкенде (например `GET
     /images/download?url=...`), который сам скачивает байты изображения (httpx уже
     зависимость через SDK) и отдаёт их с `Content-Disposition: attachment` — тогда ссылка
     на скачивание становится same-origin, и `download` в браузере снова работает
     предсказуемо, независимо от CORS-политики ranobelib.me. Коммиты: подтверждение причины
     (диагностика выше, проверить в девтулзах, что `download` игнорируется как
     кросс-доменный) → эндпоинт-прокси скачивания на бэкенде с валидацией, что `url` ведёт
     на ranobelib.me (не открытый редирект/прокси на произвольный адрес) → переключение
     `.image-lightbox__download` на этот эндпоинт вместо прямой кросс-доменной ссылки →
     проверка, что скачивание запускается, а не открывает новую вкладку.

144. **Баг: фон превью в настройках чтения растянут на всю ширину, а не по ширине текста**
     — `.reader-settings-preview` (`app.css`, панель «Так будет выглядеть текст главы…» в
     `/settings/reading`) задаёт фон/рамку без ограничения по ширине — растягивается на всю
     доступную ширину колонки настроек, тогда как сам текст внутри (`.reader-settings-preview
     p { max-width: var(--reader-width); }`) ограничен шириной, которую как раз настраивает
     ползунок «Ширина области чтения» (PR 34) — из-за этого демонстрация настройки выглядит
     недостоверно: прямоугольник-фон не отражает то, что регулирует сам ползунок. Ограничить
     сам `.reader-settings-preview` (не только внутренний `<p>`) той же `max-width: var(
     --reader-width)`, чтобы видимый прямоугольник плотно облегал текст и реально
     демонстрировал выбранную ширину области чтения. Коммит: `max-width: var(--reader-width)`
     на `.reader-settings-preview` (убрать растягивание на всю ширину колонки настроек),
     проверить на минимальном и максимальном значении ползунка ширины.

## Что явно не делать

- Авторизацию/логин на ranobelib.me — наша своя авторизация (PR 13) уровня приложения к
  этому не имеет отношения и никак не интегрируется с аккаунтом на ranobelib.me.
- Запись/загрузку контента на ranobelib.me (SDK и API read-only).
- Парсить каталог RanobeLib в обход SDK — ни постоянно, ни как временный воркэраунд. Раздел
  «Все тайтлы» (PR 15) реализуется только после появления метода листинга каталога в самом
  SDK; до тех пор PR остаётся заблокированным, а не обходится самодельным скрапингом.
- Иллюстрации в fb2/txt-экспорте — ограничение самого SDK, не создавать в UI впечатление,
  что это возможно для этих двух форматов (не показывать превью картинок как будто они
  попадут в fb2/txt).
- Свой кэш метаданных/глав поверх SDK-кэша (это отдельно от БД приложения из PR 13 —
  personal-library/активность/пользователи не кэш SDK-данных, а собственные данные
  приложения, см. раздел «Архитектура»).
- Хардкод списка форматов экспорта.
- Парсинг русского названия тайтла или расчёт объёма/веса новеллы на веб-слое без
  предварительной проверки, что этого нет в актуальной версии SDK (см. PR 25, PR 29 и
  Golden rule выше) — сначала проверка SDK/issue, потом, если необходимо, воркэраунд.
- Хардкод списка жанров (id → название) на веб-слое ради чекбоксов фильтра в каталоге
  (см. PR 38) — в SDK `0.6.1` нет публичного метода листинга жанров, значит сначала issue
  в репозитории SDK, PR остаётся заблокированным до его релиза, а не обходится своим
  захардкоженным списком.
- Хардкод списка стран на веб-слое ради раздела «Страна» в панели «Фильтры» (см. PR 85) —
  тот же принцип, что и с жанрами: в SDK `0.7.0` нет ни поля страны, ни метода её листинга,
  значит сначала issue в репозитории SDK, раздел остаётся заблокированным до его релиза.
- Фильтрация по тегам или по автору/художнику/переводческой команде (см. PR 86, PR 87)
  через выкачивание всего каталога и ручную фильтрацию на веб-слое в обход отсутствующего
  параметра в `Catalog.list_titles()` — сначала issue в репозитории SDK на нужный параметр,
  а не самодельный обход через массовую загрузку и локальную фильтрацию результатов.
- Мультивыбор страны (см. PR 100) через несколько последовательных вызовов
  `Catalog.list_titles(country=...)` (по одному на отмеченную страну) с объединением
  результатов на веб-слое — `country` в SDK `0.8.0` принимает только один id; сначала issue
  в репозитории SDK на список стран, PR остаётся заблокированным до его релиза, а не
  обходится N запросами и ручной склейкой страниц/пагинации.

## Git workflow

Один пункт roadmap выше = один PR. Один PR = набор атомарных коммитов (см. разбивку под
каждым пунктом roadmap), а не один гигантский коммит на всю фичу. Не смешивать в одном PR
изменения из разных пунктов roadmap, даже если технически проще. Сообщения коммитов —
consistently на одном языке (английский или русский) на весь репозиторий.

## Тесты

Мокать SDK на границе `services/` (не бить в реальный ranobelib.me в тестах). Отдельно
покрыть маппинг исключений — самая критичная для UX часть логики, легко сломать молча.
